# REF-028: Execute measurement-driven performance improvements

Status: `complete`

Date: 2026-08-10

## Objective

Re-measure the modularized renderer and Player subscription boundaries, then accept only changes with a reproducible material improvement while preserving route output, data freshness, subscription cleanup, error/fallback semantics, and existing on-demand imports.

## Measurement method

`npm run measure:renderer-bundle` performs a production-config Vite/Rollup build entirely in memory (`write: false`) and reports exact emitted and gzip bytes, static/dynamic chunk edges, and the largest rendered module contributors. It does not start the renderer, Electron, the API, Firebase, or any hosted service.

The ordinary release configuration is intentional for bundle fidelity: setting `VITE_ENABLE_FIREBASE_SYNC=false` at compile time changes tree shaking and does not represent the shipped default. Runtime checks, if any become necessary, must instead use disabled Firebase sync and an unreachable loopback API.

## Baseline

The post-REF-027 production build and in-memory measurement agree:

| Artifact | Emitted bytes | Gzip bytes | Loading boundary |
| --- | ---: | ---: | --- |
| Renderer entry | 1,009,541 | 301,459 | startup |
| Renderer CSS | 188,848 | 34,045 | startup |
| ExcelJS | 1,066,528 | 268,727 | existing on-demand spreadsheet import |
| ZXing | 412,054 | 107,682 | existing on-demand QR-scanner import |

The entry contains 196 modules. Its largest unavoidable current owners are Firestore, React DOM, and Firebase Auth. The nine non-default management route components together contribute 194,351 rendered bytes before minification:

| Route component | Rendered bytes |
| --- | ---: |
| `SummaryView.tsx` | 48,712 |
| `ProfilesView.tsx` | 43,555 |
| `SettingsView.tsx` | 31,801 |
| `TournamentsView.tsx` | 24,499 |
| `BuilderView.tsx` | 12,021 |
| `TableView.tsx` | 11,614 |
| `TournamentTvView.tsx` | 10,313 |
| `SignalsView.tsx` | 7,390 |
| `KpisView.tsx` | 4,446 |

This is a measured route/feature lazy-loading candidate. `FloorView` remains the default signed-in route and is not a deferral candidate.

## Characterization gate

Before changing production imports, the established REF-006 route contract suites passed 10 files / 36 tests. They cover Outreach, Tonight/builder, Profiles, Tournaments, Reports/KPIs, Settings, Table, Floor, tournament lifecycle, and night-close lifecycle DOM/callback behavior.

The Player subscription candidate was inspected but not selected: it already rejects overlapping refreshes, pauses the polling timer outside the active app state, refreshes on foreground return, and tears down parent/child listeners and timers. Its existing lifecycle tests cover those policies. No measured redundant workload supports changing freshness or polling behavior.

## Acceptance threshold

The route-loading change will be retained only if the production-config measurement materially reduces startup JavaScript bytes and all characterized routes settle to their unchanged output. ExcelJS and ZXing must remain on-demand. Any candidate without a material measured gain will be reverted rather than committed.

## Accepted change

The default Floor route remains a synchronous import. Builder, KPIs, Profiles, Settings, Signals, Summary, Table, Tournaments, and Tournament TV now use `React.lazy` behind a shared `Suspense` boundary. Each route is fetched only when selected; the shell and its notification controls remain mounted while route content loads. The tournament-TV window uses the same loading boundary without changing its empty-state behavior.

The route harnesses preload the component under test so their existing final DOM, callback, state, and lifecycle assertions remain unchanged. `rendererRouteLoading.test.ts` prevents the default route from becoming asynchronous and prevents the nine non-default route imports from returning to the startup entry.

## Result

| Artifact | Before emitted | After emitted | Before gzip | After gzip |
| --- | ---: | ---: | ---: | ---: |
| Renderer entry | 1,009,541 | 915,550 | 301,459 | 281,370 |
| Renderer CSS | 188,848 | 188,848 | 34,045 | 34,045 |
| ExcelJS | 1,066,528 | 1,066,528 | 268,727 | 268,728 |
| ZXing | 412,054 | 412,054 | 107,682 | 107,682 |

Default startup JavaScript decreased by 93,991 emitted bytes (9.31%) and 20,089 gzip bytes (6.66%). The nine route chunks total 95,069 emitted / 25,065 gzip bytes; four shared icon chunks total 1,416 / 1,000 bytes, and only the chunks needed by a visited route load. If every route and existing lazy feature is eventually loaded, emitted JavaScript grows by 2,494 bytes (0.10%) and gzip transfer by 5,977 bytes (0.88%) from chunk/import overhead. This bounded tradeoff is accepted for the material default-startup reduction.

ExcelJS keeps its exact emitted size; the one-byte gzip variance comes from its rewritten entry-chunk reference. ZXing and CSS are byte-identical. No chunk warning threshold, dependency, freshness policy, or runtime data operation changed.

## Atomic commits

- `3b80c15` — write-free bundle measurement command, baseline, and route characterization record.
- `2229a4d` — defer the nine measured non-default routes and add the ownership regression.
- `0c715ba` — make every affected non-default-route harness await its deferred component.

## Verification

- PASS pre-change route characterization: 10 files / 36 tests.
- PASS post-change compiler gate: all four root compiler projects.
- PASS post-change route/loading gate: 11 files / 37 tests.
- The first full verification exposed one additional Profiles-route harness that asserted before its lazy import settled; no product assertion ran against the loaded route. The complete harness audit added four missing preloads, and its affected gate passed 5 files / 16 tests.
- PASS standalone production renderer build and in-memory bundle measurement with matching artifact bytes.
- PASS final `npm run verify`: all four root compiler projects, Player TypeScript, 81 test files / 471 tests, and the 1,957-module renderer build.
- Known SQLite experimental, ExcelJS `eval`, and remaining large-chunk warnings are unchanged in kind; no warning was suppressed.

No production-connected runtime, provider, credential, database, deployment, native workflow, dependency change, or push was used.
