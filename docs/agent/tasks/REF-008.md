# REF-008 - Extract Electron process modules

Status: complete

## Objective

Make `electron/main.cjs` primarily wire reviewed Electron/Node modules while preserving the preload bridge, IPC contract, SQLite shapes, API/Firebase fallback order, sync protocol behavior, update flow, security settings, and failure semantics.

## Preconditions and safety

- TYPE-016 is complete; `electron/main.cjs`, `electron/preload.cjs`, and `electron/firebaseSync.cjs` pass dedicated non-DOM check-JS.
- Every production move from `electron/main.cjs` requires focused characterization committed against the unchanged owner first.
- This task must not launch Electron, read a pilot private key, run `npm run stress:e2e`, contact hosted APIs/Firebase, send messages/email, publish an update, or change a database/schema.
- Module APIs remain process-local CommonJS. Renderer/preload exposure does not expand.
- Firebase/player request transformation remains in `main.cjs` until REF-009 decides and characterizes shared sync ownership.

## Sequence

| Subtask | Scope | Required characterization |
| --- | --- | --- |
| REF-008A | Pure error, validation, outreach-message, and account-key utilities | exact errors, field preservation, filtering/cap/order, precedence/fallback, immutability |
| REF-008B | API client and client telemetry | URL/auth headers, timeout/error projection, API-first/fallback results, telemetry payload/event routing |
| REF-008C | Local SQLite state and analytical reports | paths, legacy/account reads, writes, report validation/storage/email fallback, row/result shapes |
| REF-008D | Embedded localhost HTTP backend | localhost binding, routes/statuses, request limits, auth/account isolation, delegated mutations |
| REF-008E | Auto-update orchestration | status/event sequence, state-preservation handshake, idempotent install, timer/listener ownership |
| REF-008F | Final window/IPC composition audit | exact IPC channels, navigation allowlist, sandbox/context isolation/node-integration invariants |

## Progress

### REF-008A - Runtime utilities

- Characterization commit: `ae7882b` (`test: characterize Electron runtime utilities`).
- Implementation: moved pure record/error helpers, state validation, outreach-batch normalization, and account-key derivation into `electron/runtimeUtils.cjs`; `electron/main.cjs` imports the same process-local CommonJS API.
- The focused tests load the extracted module directly while retaining the existing source-isolation harness for runtime owners not yet moved.
- Verification after implementation: `npm run typecheck:electron`, the focused six-test file, `npm run typecheck`, `npm run build`, and `npm run verify` all passed. The full gate covered 41 files / 214 tests and 1,930 renderer modules.
- No Electron process, hosted endpoint, Firebase path, messaging provider, updater, or database was invoked.

### REF-008B - API client and client telemetry

- Characterization commits: `0735d60` (`test: characterize Electron API client boundary`) and `5478ec4` (`test: characterize Electron telemetry identity`).
- Implementation: moved authenticated API transport, pilot/state/report projections, device identity, client telemetry state/routing/timer ownership, and API-first fallback orchestration into `electron/orbitApiClient.cjs`.
- The process-local factory receives local-state, Firebase, reporting, clock, timer, device-path, identity, logging, and transport dependencies explicitly. `electron/main.cjs` retains IPC, local SQLite/report owners, player-sync transformations, and lifecycle composition.
- The characterization suite now loads the extracted module directly and covers 14 API/fallback/telemetry cases; the companion Electron findings suite contributes another six focused cases.
- Verification after implementation: both focused files (20 tests), `npm run typecheck`, and `npm run verify` passed. The full gate covered 42 files / 228 tests and 1,930 renderer modules.
- No Electron process, hosted endpoint, Firebase path, messaging provider, updater, or database was invoked; all transport, persistence, timer, and fallback collaborators were isolated fakes.

### REF-008C - Local SQLite state and analytical reports

- Characterization commit: `b4af6ea` (`test: characterize Electron local storage`).
- Implementation: moved user-data paths, SQLite connection/schema/lifecycle ownership, account and legacy reads, transactional state/profile projection, analytical-report validation/storage, optional endpoint/SMTP delivery, and email formatting into `electron/localStore.cjs`.
- The factory accepts filesystem, SQLite constructor, time/identity, transport/mailer, environment, and backend-status collaborators. `electron/main.cjs` only updates the embedded-backend report-count projection and composes the returned read/write/report operations.
- The 14 direct local-store tests cover connection close/reopen in addition to the characterized paths, migrations, account isolation, transaction ordering/rollback, profile rows, companion filtering, delivery ordering, email shape, and stored/queued result metadata.
- Verification after implementation: three focused Electron files (34 tests), `npm run typecheck`, and `npm run verify` passed. The full gate covered 43 files / 242 tests and 1,930 renderer modules.
- The tracked `data/orbit-api.sqlite3` was not opened or altered. No Electron process, report endpoint, SMTP server, hosted API, Firebase path, or other production integration was invoked; every boundary used an isolated fake.

### REF-008D - Embedded localhost backend

- Characterization commit: `aa35453` (`test: characterize Electron embedded backend`).
- Implementation: moved JSON/CORS responses, bounded request-body reads, server/status lifecycle, bind configuration, loopback/LAN policy, health and player/report routes, and delegated persistence into `electron/embeddedBackend.cjs`.
- The controller owns start/stop/status/report-count state. `electron/main.cjs` injects the existing membership/waitlist transforms, player snapshot builder, Firebase-aware load/sync/save composition, and local report store; those sync behaviors remain in `main.cjs` for REF-009.
- The trust boundary remains unchanged: non-loopback requests receive 403 unless `TABLEMANAGER_PLAYER_SYNC_ALLOW_LAN=true`; there is no separate embedded-server auth layer. Snapshot and mutation lookups remain scoped by sanitized query account key or payload `clubId`.
- The eight direct controller tests capture a fake request handler and never bind a socket. They cover headers, the two-million-character limit, configured/default lifecycle status, loopback/LAN behavior, account/player scoping, all route success/missing/error statuses, mutations, and report delegation.
- Verification after implementation: four focused Electron files (42 tests), `npm run typecheck`, and `npm run verify` passed. The full gate covered 44 files / 250 tests and 1,930 renderer modules.
- No Electron process, TCP listener, tracked database, hosted API, Firebase path, report endpoint, or SMTP server was invoked.

### REF-008E - Auto-update orchestration

- Characterization commit: `1a1fc74` (`test: characterize Electron update orchestration`).
- Implementation: moved update status broadcasts, renderer state-flush request/ack/timeout handling, local-first preservation, update installation guards/delay, updater configuration/listeners/check timer, and timer cleanup into `electron/updateController.cjs`.
- `electron-updater` continues to own checks/download/install; Electron's native updater remains the owner of `before-quit-for-update`. `electron/main.cjs` injects window selection, telemetry, local/API persistence, and logging, then delegates the preservation IPC handler and lifecycle start/stop.
- The eight direct controller tests cover live-window broadcast, authoritative-window selection, acknowledgement and timeout, duplicate download/install idempotency, development/unpackaged bypass, exact configuration/emitter/event/status/progress routing, timer cleanup, check failures, and local/cloud preservation outcomes.
- The TYPE-016 regression assertion now inspects the extracted owner, so the native-emitter correction remains pinned alongside the behavioral suite.
- Verification after implementation: the updater and compiler-finding files (14 tests), `npm run typecheck`, and `npm run verify` passed. The full gate covered 45 files / 258 tests and 1,930 renderer modules.
- No Electron process, updater check/download/install, release endpoint, renderer window, tracked database, hosted API, or Firebase path was invoked.

### REF-008F - Window, IPC, and lifecycle composition audit

- Characterization/audit commit: `04b8865` (`test: characterize Electron composition boundary`).
- The final audit required no production edit. `electron/main.cjs` remains the composition owner for the exact IPC registration set, window and application lifecycle, route selection, trusted navigation, and process-local module wiring.
- Six static composition tests pin the exact 11 preload/invoke channels, preload argument and event forwarding, renderer route normalization, trusted external protocols, development/packaged route hashes, BrowserWindow isolation settings, popup denial, and navigation allowlist.
- Firebase/player request transformation remains deliberately in `electron/main.cjs` until REF-009 establishes shared sync ownership. IPC/window/lifecycle composition and outreach transport/logging also remain cohesive main-process responsibilities.
- Final verification: Electron check-JS, both root compiler projects, Player TypeScript, 46 files / 264 tests, the 1,930-module renderer build, and `npm run verify` all passed.
- No Electron process, socket, updater, tracked database, network endpoint, SMTP provider, hosted API, or Firebase path was invoked.

## Acceptance

- Characterization and implementation remain separate commits for each risky boundary.
- `electron/main.cjs` primarily composes imports, Firebase-request synchronization, IPC handlers, windows, and application lifecycle.
- Extracted modules have explicit dependency injection where it prevents hidden process/global coupling and can be tested without Electron or external services.
- Electron check-JS, focused tests, both root compiler projects, Player TypeScript, all unit tests, renderer build, and `npm run verify` pass after every implementation subtask.
- Documentation records any cohesive responsibility deliberately left in `main.cjs`; no production-connected harness or deployment is run.
