# Orbit Player production change proposal

Prepared September 6, updated September 7, 2026. **All three approved operations are complete: the first candidate was rolled back, the diagnostic was deleted after identifying the managed credential, and the final merged API candidate is retained and build-verified.** Historical proposal text and exact source inputs are retained for audit. Further production mutations require their own stated scope and rollback.

September 7 API attempt: the user approved the first candidate. Deployment `dpl_4KoTNHWQboGvHv14aguYMCNDVQzx`, from PR #26 merge `9458a6a50567c879b537631b9b25fb76e89b4133`, reached READY on Node 22.x, but Vercel reassigned the secondary API alias despite `--skip-domain`. The secondary alias was restored to its exact prior deployment and the candidate deleted. All four aliases and saved API build settings match the original snapshot at `2026-09-07T01:26:25.641Z`. No runtime requests were sent by this run; incidental traffic or data effects during the brief secondary-alias assignment are not established by the control-plane evidence. No credential diagnostic output was observed. The revised diagnostic was subsequently approved, completed, and deleted; see the latest execution record.

Verified PR #26 squash merge: `9458a6a50567c879b537631b9b25fb76e89b4133`, merged at `2026-09-06T22:45:47Z`, with successful actual-merge [CI 34064957418](https://github.com/sahusgupta/orbit/actions/runs/34064957418). The staging-only Web repair in PR #27 subsequently passed every PR check and merged, with explicit user approval, at `8d086bfd689b7f0af1037468c0764d16b32d77bc` on `2026-09-07T00:37:51Z`. Both API subtrees are `87570aa5b2e8bce56c11161fabebdf319e23a74f`. **The first API attempt used exactly the PR #26 merge and prepared directory below, under the later explicit API-candidate approval.**

The user approved the revised diagnostic-only build. It ran from PR #26 merge `9458a6a50567c879b537631b9b25fb76e89b4133`, stopped during installation with the intended exit 42, and was deleted. All four live aliases and saved API build settings remained unchanged. The current managed Vercel credential belongs to `tabletalk-s`, service account `firebase-adminsdk-fbsvc@tabletalk-s.iam.gserviceaccount.com`, key `fb29d68eaf68bef1fe0dcbf58a8862721d3a503e`. Its email was redacted in the observed build log; the service-account identity was independently established by matching the exact key ID to its IAM resource. No secret value was returned, and no runtime request was sent. The older quarantined key `bffbadd088e4b1f7b93dd7fa9cfb58c49cc2a259` is separate, still enabled, and has recorded recent authentication; its consumers remain unresolved. Both diagnostic approvals are complete. Further deployment/routing, runtime testing, credential mutation, and App Check remain outside those approvals.

## Approved retained API candidate: completed

The user-approved retained API candidate is READY: `dpl_3ChFg6JQ9e3DjfGpwT2vMe9iskcZ`, immutable `orbit-l5lr0hmcw-sahoosegs-projects.vercel.app`, from final merge `8d086bfd689b7f0af1037468c0764d16b32d77bc`. Locked installation and the API build passed on Node 22.x; the provider added 261 packages, audited 262, and reported zero vulnerabilities. All four live aliases and saved API/protection settings match the preflight. No alias movement was observed, no restore command was needed, and final candidate alias metadata is empty. No runtime request or canonical cutover was performed.

The user explicitly approved the prepared deployment and rollback. The reviewed deployment command was executed once; its conditional rollback was not needed. Both are retained below as historical scope, not a request to deploy again. The earlier API subtree was unchanged, but this retained candidate traces to the final PR #27 merge.

Read-only project metadata at `2026-09-07T04:31:13.783Z` reports Vercel Authentication `deploymentType=all_except_custom_domains`. The complete project-domain list at `2026-09-07T04:31:37.165Z` contains only `orbitapp-one.vercel.app`; the secondary alias is not a configured project domain. Combined with [Vercel's Standard Protection documentation](https://vercel.com/docs/deployment-protection#standard-protection), this indicates that generated URLs should require authentication. This is an inference from settings, not a runtime protection test. Existing authorized users, bypass mechanisms, or provider behavior can still produce invocations. No bypass secret or customer traffic was inspected.

The first attempt demonstrated that `--prod --skip-domain` still assigns `orbitapp-sahoosegs-projects.vercel.app` when this API build becomes READY. The flag is documented around [custom-domain assignment](https://vercel.com/docs/cli/deploy#skip-domain); it is not an isolation guarantee for this project. This proposal explicitly includes that transient secondary-alias movement and immediate restoration on detection.

### Exact mutation and preserved rollback

1. Immediately before execution, recheck all four recorded aliases, API project/root, saved build settings, protection scope, and configured domains. Stop on drift before creating a deployment.
2. Create **one** production-environment deployment on API project `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`, team `team_CdmC6YZS1GNIoI0waQ2G01ED`, from the staged immutable merge archive. The sole packaging override adds `installCommand: npm ci` to staged `apps/api/vercel.json`; no diagnostic, application-source, dependency, or saved project-setting change is included. Inherit existing managed production settings without pulling their values. Override only deployment-local `ORBIT_RELEASE_SHA` with the exact merge and `ORBIT_ALLOWED_ORIGINS` with the three origins in the command below; attach matching `orbitReleaseSha` metadata.
3. Monitor build/control-plane state. When the secondary alias moves to the new deployment, immediately restore it to `dpl_FpyDXa1UW2q7MLc9HChYAjE93J3c` using its original immutable URL. Do not wait for manual review to perform that restore. Verify again after terminal build state so a later assignment is not missed. Preserve the canonical API and both Web aliases at their separately recorded targets.
4. Retain the new immutable candidate only if READY, the locked API build and correct source/project/runtime are verified, and all four aliases and saved settings/protection match the preflight. Inspect selected metadata and build output only. On failure, restore any changed alias and delete only the new candidate. No automatic deployment retry is included.
5. Runtime smoke, canonical API cutover, Web deployment, managed-variable changes, Firebase rules/TTL, credential remediation, and App Check enforcement remain separate scopes. This candidate alone does not close API compatibility or release gates.

**Data limitation:** the candidate can be invoked by users with access or existing bypass paths while retained, and the secondary alias could receive traffic during its transient assignment. Application middleware can create durable quotas and drain pending account-deletion or identity cleanup even on health requests. No intentional runtime request is proposed. Restoring aliases or deleting the deployment cannot undo such data effects; the prior attempt's aggregate invocation query was unavailable on the current plan. Approving this scope must explicitly accept this possibility.

Prepared directory: `C:/Users/herob/AppData/Local/Temp/orbit-api-candidate-8d086bfd-hJ5lW1`. Archive SHA-256: `066361a75ddb30344f924bca35fc9e3ea36cd4c8266aff67b8ce89d765afc795`; staged config SHA-256: `3a631bf706ba0f638ff0917dcd331f32f9e05d249c0c63dc6a4c2b5fc871ba35`; API lock SHA-256: `5367352d5e0bbb226d74d7b38e01b7a5dcdb32b933e32b39603edee140b1b389`. The root branding file remains the exact archive blob. No live environment file, credential diagnostic, generated bundle, cache, or dependency directory is staged. CLI dry run passed for **112 files / 1,289,762 bytes**, with every file hash matched. No upload occurred. The only ignored archive file is `apps/api/.gitignore`; the included `apps/api/.env.example` is the exact committed template.

Exact command (existing authenticated Vercel CLI 59.11.7; run from the prepared directory):

```powershell
vercel deploy 'C:/Users/herob/AppData/Local/Temp/orbit-api-candidate-8d086bfd-hJ5lW1' --project prj_2EEyDB2YImgK9dil3GK79Qy0UfVw --scope sahoosegs-projects --local-config 'C:/Users/herob/AppData/Local/Temp/orbit-api-candidate-8d086bfd-hJ5lW1/apps/api/vercel.json' --prod --skip-domain --env 'ORBIT_RELEASE_SHA=8d086bfd689b7f0af1037468c0764d16b32d77bc' --env 'ORBIT_ALLOWED_ORIGINS=https://orbit-player-web.vercel.app,https://orbit-player-web-liart.vercel.app,https://orbit-player-web-sahoosegs-projects.vercel.app' --meta 'orbitReleaseSha=8d086bfd689b7f0af1037468c0764d16b32d77bc' --yes --no-wait --no-color
```

Required secondary restoration, both normal completion and failure rollback:

```powershell
vercel alias set orbit-f6o4olsbb-sahoosegs-projects.vercel.app orbitapp-sahoosegs-projects.vercel.app --scope sahoosegs-projects
```

Failure cleanup after exact new ID verification: `vercel remove <new-candidate-id> --scope sahoosegs-projects --yes`. If the canonical alias unexpectedly changes, restore only `orbitapp-one.vercel.app` to `orbit-e18gsa0uo-sahoosegs-projects.vercel.app`; if either Web alias unexpectedly changes, restore it to `orbit-player-kzmuv190g-sahoosegs-projects.vercel.app`. Preserve every prior deployment. No broad promote/rollback command is part of this scope.

Evidence is local under ignored `out/release-20260906/`: `retained-api-candidate-proposal.json`, `retained-api-candidate-dry.json`, `api-protection-metadata.json`, and `api-project-domain-metadata.json`. Existing full verification and actual-merge CI passed on the unchanged source; no additional application test is justified for this documentation/packaging-only preparation. Release-document validation and link/manifest checks are recorded in the execution ledger.

## Verified current state

| Boundary | Observed state |
| --- | --- |
| Canonical API | `orbitapp-one.vercel.app` → `dpl_CJjBqJEKrsH6sxu2mwd22m3UwWWk`, immutable `orbit-e18gsa0uo-sahoosegs-projects.vercel.app`; source metadata `fb01a452a72d33510ee06c49c560441c9b51307c`, August 24. |
| Secondary API alias | `orbitapp-sahoosegs-projects.vercel.app` → **different** deployment `dpl_FpyDXa1UW2q7MLc9HChYAjE93J3c`, immutable `orbit-f6o4olsbb-sahoosegs-projects.vercel.app`; source `88544b13d5cfa049b0841a9bcf48dfc7cd17de3d`, August 29. Do not accidentally move this alias with a broad promotion. |
| Player Web | Both `orbit-player-web-liart.vercel.app` and `orbit-player-web-sahoosegs-projects.vercel.app` → `dpl_7A8Ptm4BcUEnkgY8gBvqKbGvj3pa`, immutable `orbit-player-kzmuv190g-sahoosegs-projects.vercel.app`, August 14; no source SHA in observed metadata. |
| Project identities | API `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`; Web `prj_22SN9L7TvAKHTye7x6PZmBhvNkoq`; team `team_CdmC6YZS1GNIoI0waQ2G01ED`. API Git deployment is disabled by committed `apps/api/vercel.json`; Web has no linked Git repository. |
| CORS | OPTIONS `/player/discovery` returned 403 without allow-origin for both real Web origins and `https://untrusted-release-probe.invalid`. A read of only the non-sensitive `ORBIT_ALLOWED_ORIGINS` field confirms an opaque entry that is not an origin, followed by obsolete `https://orbit-player-web.vercel.app`. The candidate must replace the invalid entry while preserving the existing valid host and adding the two actual hosts. |
| Client/API contract | Offline comparison against canonical deployment source metadata `fb01a452a72d33510ee06c49c560441c9b51307c` shows that source lacks the candidate identity-capture, membership-QR issue/redeem, tournament-interest, and `/version` routes. This is a source-based compatibility finding, not a live 404 probe. The new app must not be released against the existing API. |
| Runtime | Project defaults show 24.x, but the installed Vercel 59.11.7 build resolver maps package engine `22.16.0` to `nodejs22.x`; no source/runtime downgrade is needed. Record the actual provider patch version from each candidate build. |
| Firebase | `tabletalk-s` / `133175572500`; 15 READY composite indexes. All 14 local required indexes match after normalizing Firestore's implicit `__name__` ordering. Preserve the additional legacy `privateGames` index. No TTL field is active. |
| Rules | Release `projects/tabletalk-s/releases/cloud.firestore` still uses ruleset `projects/tabletalk-s/rulesets/a74cb1f9-275a-491c-a8f7-0c513241cade`, updated August 12; saved source SHA-256 `0c1805ef988d2ea68913b65524009698bfa1f5c236785cce1779ef52cc474f21`. |
| App Check | Auth, Firestore, Storage service GETs have no configured enforcement mode, which defaults to OFF. No enforcement or debug-provider change was made. |
| API-key metadata | Dedicated Web key has the two actual referrers and five Firebase API targets. Native iOS/Android keys have empty platform allowlists and 27 Firebase-related API targets; shared browser key is separate and unchanged. No Maps API target was observed. No key string was requested or printed. |
| Credential/policy | Current managed Vercel credential identity is resolved by diagnostic key ID and IAM resource: `tabletalk-s`, `firebase-adminsdk-fbsvc@tabletalk-s.iam.gserviceaccount.com`, key `fb29d68eaf68bef1fe0dcbf58a8862721d3a503e`. The older quarantined key remains enabled with recent recorded use. No key value was read locally. `ORBIT_ACCOUNT_DELETION_POLICY_JSON` remains absent. |

## Concrete local input prepared from the merge

The isolated API staging directory is `C:/Users/herob/AppData/Local/Temp/orbit-release-9458a6a50567-JGcVhU/api`. Exact-source API archive SHA-256: `641a57a65c7666974946f2b0eef756bd5b1348517f39e7e4861aad36bfafec0c`. The archive includes the required root `branding.config.json`. Both package locks were checked against their immutable Git blobs; per-command LF export avoids Windows Git line-ending conversion. No source lockfile was edited.

The only packaging additions are the diagnostic below and the staged `apps/api/vercel.json` install/build overrides. Staged configuration SHA-256: `cdaeb998684fecf014a95494710fcbade61cb59fee13f342f9974c3044d5a119`. Vercel 59.11.7 `deploy --dry --json --project prj_2EEyDB2YImgK9dil3GK79Qy0UfVw --scope sahoosegs-projects --prod --skip-domain` passed for 113 files / 1290802 bytes; it uploaded nothing and created no deployment. Provenance: `out/release-20260906/merged-packaging-9458a6a.json`; dry-run report: `out/release-20260906/api-deploy-dry-9458a6a.json`.

## First proposed mutation: unaliased API candidate and build-only metadata

1. From an isolated clean checkout of the exact merge SHA, prepare the API upload using the reviewed source and its lockfile. Include a separately identified packaging-only script that parses the managed Firebase JSON **in the provider build process** and emits only `project_id`, `client_email`, and `private_key_id`. It must never print or write `private_key`, tokens, or the JSON value, import the application, or call Firebase. Record that script's SHA-256 alongside source provenance. Place it at the disposable upload root as `release-credential-metadata.cjs`; preserve the API root `apps/api`. The packaging-only Vercel configuration sets `installCommand` to `npm ci` and `buildCommand` to `node ../../release-credential-metadata.cjs`, while retaining disabled Git deployment. Review and hash that configuration before upload.
2. Create one production-environment Vercel candidate on the canonical API project with `--prod --skip-domain`. Set deployment-specific `ORBIT_RELEASE_SHA=<exact merge SHA>` and `ORBIT_ALLOWED_ORIGINS=https://orbit-player-web.vercel.app,https://orbit-player-web-liart.vercel.app,https://orbit-player-web-sahoosegs-projects.vercel.app`. This preserves the existing valid host and adds only the two real Web origins. Use a deployment-specific build-command override for the metadata script; do not alter the saved project build command or managed secret values. Keep all aliases on the deployments above. Do not enable App Check.
3. Inspect build output/runtime selection, source provenance, and the three non-secret identifiers. **Do not send runtime requests under this first approval:** current request middleware can drain pending deletion/provider-cleanup jobs and write durable quota records, even for health requests. Candidate runtime verification therefore requires the separately reviewed production-data scope below.

Original expected rollback: delete only an unaliased candidate and the disposable local staging files, with no alias, managed environment, Firebase, credential, or enforcement changes. The alias expectation was not met; the actual restore-and-delete rollback is recorded below. The original live deployments retain their original environments. The installed Vercel 59.11.7 deployment implementation passes local configuration overrides in the create-deployment request and sets `autoAssignCustomDomains=false` for `--skip-domain`. It does not require editing the saved project build command. Verify provider metadata after creation; if any override requires a saved project setting change, stop and report the revised mutation first.


## Revised diagnostic-only failed build (approved, completed, deleted)

The user explicitly approved this revised operation with "yes". The following reviewed scope was executed once and completed with the expected install exit 42, no alias assignment, resolved managed credential identity, and verified deletion. It does not solve the live API/CORS cutover or prove runtime behavior. These steps are historical execution scope, not permission to repeat the build.

- Target remains API `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`, team `team_CdmC6YZS1GNIoI0waQ2G01ED`, production environment. Prepare one diagnostic-only build from the same immutable PR #26 archive, with the same two deployment-only environment overrides and `--prod --skip-domain`.
- Prepared upload: `C:/Users/herob/OneDrive/Documents/GitHub/TableManager/out/release-20260906/api-diagnostic-retry-8mYr6Q`. The exact diagnostic SHA-256 is `1659eb901aed854f3ec0833c42284492fd52ee29802ef6808fcc6cc57668e10e`; staged config SHA-256 is `54b29e10fcedf19305d66a848ce29dc909d1ae1ef0e94f906b96ef7831ed542b`. API lock remains `5367352d5e0bbb226d74d7b38e01b7a5dcdb32b933e32b39603edee140b1b389`. No API source or package script is edited.
- The reviewed diagnostic is unchanged except for a final `process.exitCode = 42`. It prints only `project_id`, `client_email`, and `private_key_id` when their project/format validation passes. Missing, malformed, or wrong-project input exits 1 without echoing it. It imports no application, makes no network request, and writes no credential file.
- Set both deployment-only `installCommand` and `buildCommand` to `node ../../release-credential-metadata.cjs`. Installation was directly observed to honor its override in the first attempt. A valid diagnostic must end with install exit 42 and provider ERROR; there should be no application function output. This deliberate failure is the stop condition, not a CI pass or deployable API. Do not retry if the expected output is absent.
- Inspect only build/control-plane metadata, preserve the three identifiers, verify all four alias mappings and saved project settings, then delete this one failed deployment. No runtime requests, secret rotation, Firebase data/rules/TTL mutation, managed environment changes, or App Check activation are included.
- Rollback: if the provider unexpectedly changes an alias, restore only that alias to the exact recorded deployment above immediately; delete the diagnostic deployment. Preserve the original deployments and managed settings. No broad promotion is permitted.

Four synthetic tests passed locally (valid identifiers/exit 42, malformed JSON, wrong project, missing credential). CLI dry run passed for 113 files / 1,290,951 bytes with no upload/deployment. Evidence: `out/release-20260906/api-diagnostic-retry-proposal.json` and `api-diagnostic-retry-dry.json`.

The earlier no-alias assumption is withdrawn: the installed CLI passes `autoAssignCustomDomains=false`, but the system-generated secondary alias still moved. [Vercel documents the flag as overriding custom-domain assignment](https://vercel.com/docs/cli/deploy#skip-domain); live evidence governs this project. The installed Node builder executes a project build command only when its `considerBuildCommand` branch is enabled; [its source](https://github.com/vercel/vercel/blob/main/packages/node/src/build.ts) and the missing diagnostic output explain why the stored command is insufficient evidence of execution. The exact provider internal branch was not observed. A retained API candidate still needs a separately reviewed alias-isolation method or explicitly approved alias scope.

## Subsequent mutations require their own concrete authorization

| Proposed change | Prerequisite and precise scope | Rollback / irreversibility |
| --- | --- | --- |
| Credential cleanup | The current managed key `fb29d68eaf68bef1fe0dcbf58a8862721d3a503e` differs from older quarantined key `bffbadd088e4b1f7b93dd7fa9cfb58c49cc2a259`. Both are enabled; the old key has recent authentication. Identify and migrate its consumers before a separately approved disable operation on its exact IAM key resource. Replacing the current Vercel variable alone does not retire the old key. Do not delete either key or quarantine. | If disabling the old key later breaks a confirmed consumer, its exact `:enable` IAM operation is the incident rollback, requiring explicit approval because it reopens credential exposure. Key deletion/quarantine destruction is irreversible and remains separate. |
| Runtime and functional candidate smoke | Explicitly authorize candidate requests, durable rate-limit writes, existing pending-finalizer processing, and a clearly named synthetic reviewer account/venue. Deletion smoke also needs the counsel-approved policy value and configured identity providers; the schema example is not a legal decision. | Remove only test-created data through the reviewed deletion path. Existing pending deletions and identity-provider cleanup cannot be undone by alias rollback. No real-player data should be copied to evidence. |
| Canonical API cutover | After all critical smokes pass, assign only `orbitapp-one.vercel.app` to the exact verified candidate. Preserve the secondary API alias. Include HTTPS public legal-route checks and real CORS allowed/rejected probes. | Assign `orbitapp-one.vercel.app` back to `orbit-e18gsa0uo-sahoosegs-projects.vercel.app`. Deployment rollback does not restore deleted data. |
| Player Web candidate/cutover | Stage the exact merge source with `scripts/stage-player-web-deploy.mjs`, locked install, required shared modules, existing public production configuration, and no secret/build/cache files. Create an unaliased production candidate first. Verify the deployed reCAPTCHA/referrer behavior on approved hosts before assigning the two exact Web aliases. | Assign both Web aliases back to `orbit-player-kzmuv190g-sahoosegs-projects.vercel.app`. Do not broadly promote unrelated aliases. |
| Firestore rules | Publish the reviewed `player-app/firestore.rules` only after comparing active-client operations and passing the isolated eight-test rules suite. Keep a reference to the prior ruleset and source backup. All required composite indexes already exist, so do not replace/delete the index inventory. | Point `cloud.firestore` back to the saved ruleset after validating active-client compatibility. Preserve all existing indexes. |
| Four TTL fields | Enable TTL on `expiresAt` for `orbitClientErrors`, `orbitTelemetryEvents`, `orbitClientUpdateEvents`, and `orbitRateLimits`, with the committed indexing exemptions. No historical backfill is included. | Disable those four TTL policies and restore only their prior field configurations. TTL deletion is asynchronous and irreversible; disabling it cannot restore deleted records. Any historical timestamp backfill needs its own reviewed data mutation. |
| App Check activation | Signed physical-device App Attest proof, valid deployed Web tokens, and a complete active-client inventory must precede any enforcement. A first API-only candidate may later allow exactly the proven iOS/Web App IDs; Android must not be presumed ready. Project-wide Firebase enforcement remains separate because management clients share Firestore. | Restore the recorded prior enforcement state and prior deployment if a supported client fails. Authentication, authorization, rules, and QR validation remain intact. No App Check enforcement is proposed in the first mutation. |
| Native key restrictions | Do not add an iOS bundle restriction while the Firebase JavaScript Auth transport omits `X-Ios-Bundle-Identifier`. The platform metadata inventory is complete; compatibility testing and a supported transport design are required before changing those restrictions. | Restore each key's saved restriction object, without rotating its key string. Do not change the shared management browser key. |

## Evidence and provider references

Local sanitized evidence: `out/release-20260906/firebase-metadata-after-push.json`, `vercel-alias-metadata.json`, `vercel-credential-metadata-after-push.json`, and `ci-34057170270.log`. These contain metadata/results only and are not staged.

The proposed packaging-only diagnostic is `out/release-20260906/release-credential-metadata.cjs`, SHA-256 `36d276a91dd43de545d70387a004c048c35beec3d7060aa65ff58c64abee513d`. Three synthetic tests passed: valid identifiers emit only the three allowed keys, malformed input fails without echoing it, and a different project fails without echoing it. It has not run in a provider build. A subsequent `vercel env run` metadata inspection was rejected by automatic approval because it could load production secrets; that operation was not retried. The separate API read requested only the non-sensitive CORS field.

- [Vercel sensitive variables are non-readable after creation](https://vercel.com/docs/environment-variables/sensitive-environment-variables).
- [Vercel Node major-version selection](https://vercel.com/docs/functions/runtimes/node-js/node-js-versions).
- [Firebase unconfigured App Check enforcement defaults to OFF](https://firebase.google.com/docs/reference/appcheck/rest/v1/EnforcementMode).

Apple authentication/signing, accountable legal/privacy declarations, and unavailable physical-device testing remain separate human-controlled gates. No App Review submission is authorized while those or critical technical gates remain open.

## Approved API candidate attempted and rolled back - September 7 UTC

September 7 API attempt: the user approved the first candidate. Deployment `dpl_4KoTNHWQboGvHv14aguYMCNDVQzx`, from PR #26 merge `9458a6a50567c879b537631b9b25fb76e89b4133`, reached READY on Node 22.x, but Vercel reassigned the secondary API alias despite `--skip-domain`. The secondary alias was restored to its exact prior deployment and the candidate deleted. All four aliases and saved API build settings match the original snapshot at `2026-09-07T01:26:25.641Z`. No runtime requests were sent by this run; incidental traffic or data effects during the brief secondary-alias assignment are not established by the control-plane evidence. No credential diagnostic output was observed. The revised diagnostic was subsequently approved, completed, and deleted; see the latest execution record.

- Upload inventory and SHA-1 for all 113 files / 1,290,802 bytes matched the approved dry run. The sole environment template was the exact committed `apps/api/.env.example`; no live environment file was uploaded. Five recorded archive/diagnostic/config/lock/branding SHA-256 values matched.
- Exact target: API `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`, team `team_CdmC6YZS1GNIoI0waQ2G01ED`. Command used the approved stage, explicit staged `--local-config`, `--prod --skip-domain`, the two approved deployment-only environment overrides, and `orbitReleaseSha` metadata. No saved environment values were changed.
- Created at `2026-09-07T01:23:56.450Z`; READY at `2026-09-07T01:24:20.737Z`. Immutable URL was `orbit-2z4qop31u-sahoosegs-projects.vercel.app`. Locked provider install added 261 packages, audited 262, and reported zero vulnerabilities. Provider metadata selected Node `22.x`; the exact patch was not captured. The requested build command was recorded in deployment settings, but its three-field diagnostic was absent from observed logs. The Admin credential identity remains unknown.
- Post-build alias verification failed for `orbitapp-sahoosegs-projects.vercel.app`; the canonical API and both Web aliases matched their recorded targets. Restore command: `vercel alias set orbit-f6o4olsbb-sahoosegs-projects.vercel.app orbitapp-sahoosegs-projects.vercel.app --scope sahoosegs-projects` succeeded. Then `vercel remove dpl_4KoTNHWQboGvHv14aguYMCNDVQzx --scope sahoosegs-projects --yes` removed exactly one deployment. Later inspect returned `Can't find the deployment`. No second deployment was created.
- Evidence: `api-candidate-before.json`, `api-candidate-4KoTNHWQ-metadata.json`, `api-candidate-after.json`, and `api-candidate-deletion-verified.json` in ignored `out/release-20260906/`. These contain selected metadata only. No raw managed credential was read locally or printed.
- Local diagnostic retry preparation passed four synthetic tests, including the expected nonzero exit after exactly three identifiers, plus a dry run of 113 files / 1,290,951 bytes. It is a proposed intentionally failed metadata build, not an API release. No application or lockfile source change was made. Existing full verification on the unchanged merged source remains green. Four diagnostic safety tests, the release-document contract, 32 local documentation links, manifest rollback assertions, and whitespace checks passed for this local evidence update; full verification was not rerun for documentation-only changes.

## Approved diagnostic completed and removed - September 7 UTC

The user approved the revised diagnostic-only build. It ran from PR #26 merge `9458a6a50567c879b537631b9b25fb76e89b4133`, stopped during installation with the intended exit 42, and was deleted. All four live aliases and saved API build settings remained unchanged. The current managed Vercel credential belongs to `tabletalk-s`, service account `firebase-adminsdk-fbsvc@tabletalk-s.iam.gserviceaccount.com`, key `fb29d68eaf68bef1fe0dcbf58a8862721d3a503e`. Its email was redacted in the observed build log; the service-account identity was independently established by matching the exact key ID to its IAM resource. No secret value was returned, and no runtime request was sent. The older quarantined key `bffbadd088e4b1f7b93dd7fa9cfb58c49cc2a259` is separate, still enabled, and has recorded recent authentication; its consumers remain unresolved. Both diagnostic approvals are complete. Further deployment/routing, runtime testing, credential mutation, and App Check remain outside those approvals.

- Deployment `dpl_A6qRPz4VEbHuxfi23ViEGVSEuvhz`, URL `orbit-q79iz6vvn-sahoosegs-projects.vercel.app`, target production, source metadata `9458a6a50567c879b537631b9b25fb76e89b4133`; Node `22.x`. Created `2026-09-07T03:58:12.481Z`. The installation command ran at `2026-09-07T03:58:16.688Z` and exited 42 at `03:58:16.739Z`. Provider ERROR is the intended diagnostic stop, not an application build or CI failure. The API was not installed, packaged, or probed.
- All 113 reviewed files / 1,290,951 bytes and diagnostic/config/lock hashes matched before upload. The supplied deployment settings retained both approved commands. `aliasAssigned=false`; the system alias listed as a prospective target was never assigned. Deletion removed exactly one deployment and subsequent inspect could not find it. All four aliases and saved API settings matched at `2026-09-07T04:01:11.281Z`.
- The first log parser could not parse a line containing a redacted `client_email`; extracting only validated project/key fields resolved it without retrieving the redacted value. IAM key metadata matched the full key ID to the service account. Current managed key created July 17, 2026; older quarantined key created June 29. Both have `disabled=false`. This identifies the current managed production variable, not every older immutable deployment's frozen credential.
- Read-only Cloud Monitoring key-authentication counts from June 1 through `2026-09-07T04:02:54.440Z`: older key 9 events in 3 daily buckets, latest positive bucket ending `2026-09-01T04:02:54.440Z`; current key 57,709 events in 28 daily buckets, latest positive bucket ending `2026-09-06T04:02:54.440Z`. Bucket boundaries are not exact last-use timestamps, and absence of later metrics is not proof of inactivity. Do not disable/delete the older key without resolving its consumers and separately approving the mutation/rollback.
- An audit lookup initially rejected nested service/method projection with HTTP 400. A narrower timestamp/log-name query over the old key's latest positive day completed in one page with zero entries; that does not negate authentication metrics or identify all consumers. No customer log payload, document path, IP, or token was requested.
- Aggregate Vercel function-invocation query for the first candidate's brief alias window returned `payment_required`: Observability Plus is required. No plan or billing change was made. Incidental traffic/data effects from the first attempt remain unverified.
- Selected evidence: `api-diagnostic-approved-metadata.json`, `api-diagnostic-approved-before.json`, `api-diagnostic-approved-after.json`, `api-diagnostic-approved-deletion.json`, `firebase-key-usage-after-diagnostic.json`, `firebase-old-key-audit-metadata.json`, and `first-api-attempt-invocation-metrics.json`, all under ignored `out/release-20260906/`. Provider/security handoff remains local. No source/lockfile change or Git push occurred in this continuation.

Read-only authentication-count interpretation follows [Google Cloud service-account monitoring](https://docs.cloud.google.com/iam/docs/service-account-monitoring). The counts are operational evidence, not a complete consumer inventory.

## Next proposed mutation: eight candidate runtime checks (not approved)

The retained candidate is available at deployment `dpl_3ChFg6JQ9e3DjfGpwT2vMe9iskcZ`, exact source `8d086bfd689b7f0af1037468c0764d16b32d77bc`. A read-only metadata check at `2026-09-07T08:54:13.068Z` confirms one existing automation-bypass entry; no token value was recorded. Installed CLI 59.11.7 source confirms that an explicit `--deployment` from an unlinked directory can reuse existing access without creating a bypass when missing. Full-URL `vercel curl` can generate one, so that form is excluded. No runtime command has been run.

Propose exactly eight sequential requests to this candidate only: GET `/health`, `/version`, `/privacy.html`, `/terms.html`, `/support.html`; then OPTIONS `/player/discovery` for each of the two real Web origins and `https://untrusted-release-probe.invalid`. Expect HTTP 200 and correct public content for the GETs, exact merged SHA plus no-store on version, HTTP 204 with exact allow-origin for both real origins, and HTTP 403 without allow-origin for the rejected origin. These checks establish initial startup/version/legal-serving/CORS behavior, not user-flow QA or legal approval.

Use existing authenticated Vercel CLI and its existing bypass internally, explicit deployment ID, empty unlinked working directory `C:/Users/herob/AppData/Local/Temp/orbit-api-runtime-inspect-hXb8O3`, debug/trace disabled, no redirect following and no retry. Clear inherited Vercel project/org/bypass overrides. Stop before a request if target, protection, existing access or live aliases drift. Save only selected status/content/cache/CORS fields, public version identifiers and boolean legal-content checks. Never save or print bypass values, cookies, raw request headers, or complete responses. Complete arguments and expectations are in local `out/release-20260906/retained-api-runtime-proposal.json`.

Example command (remaining seven exact argument lists are in that JSON):

```powershell
vercel curl /health --deployment dpl_3ChFg6JQ9e3DjfGpwT2vMe9iskcZ --scope sahoosegs-projects --cwd 'C:/Users/herob/AppData/Local/Temp/orbit-api-runtime-inspect-hXb8O3' --no-color -- --silent --show-error --connect-timeout 10 --max-time 30 --dump-header - --request GET
```

**Explicit production effect requiring approval:** every request can invoke the production-backed initialization, durable quota writes, and pending account-deletion/identity-cleanup drains before the selected handler runs. This possibility cannot be removed by choosing health or OPTIONS. No account creation, user-credential submission, customer-resource request, QR, tournament, or deletion endpoint is included. Counsel-approved deletion policy and later full functional QA remain separate.

On the first unexpected result, stop requests and inspect only control-plane/build evidence. On a confirmed critical startup/source/CORS defect, remove only `dpl_3ChFg6JQ9e3DjfGpwT2vMe9iskcZ` after verifying its identity and original alias mappings: `vercel remove dpl_3ChFg6JQ9e3DjfGpwT2vMe9iskcZ --scope sahoosegs-projects --yes`. Preserve all live aliases, prior deployments and saved settings. Removal cannot reverse already completed cleanup or quota writes. No redeployment, new bypass token, configuration change, cutover, credential mutation, or App Check activation is included.

The local runner is now implemented at `out/release-20260906/retained-api-runtime-runner.cjs` (SHA-256 `356f08e4d725fec324fdfe2f7dd525397c9389bba1a91b3806ced648bf042fa0`). Twenty local tests passed with zero provider/runtime requests: service/JSON validation, exact source/no-store, archived legal-page markers, allowed/rejected CORS, proxy-header handling, redirect rejection, secret-field exclusion, immutable target/eight-request limits, and approval gating. Raw command output is captured in memory and only the validated result summary is persisted. The runner stops on the first mismatch; a confirmed critical defect and the exact candidate identity must still be established before the separately specified rollback.

The runner also creates an empty task-owned `.curlrc` and sets `CURL_HOME` to that unlinked directory, so user curl defaults cannot add requests, redirects, retries or file output. Existing user configuration is not read or changed. This follows the [documented curl configuration search order](https://curl.se/docs/manpage.html#-K). No runtime execution is included in the local test result. The pending eight-request scope and irreversible-cleanup limitation are unchanged.
