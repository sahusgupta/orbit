# Orbit Player release execution — 2026-09-06

This is the active execution record for the authorized continuation of [Player App Store readiness](player-app-store-readiness.md). It supersedes that task's earlier restriction against deployment/build work for this run only. Work is ongoing. PR #25 was externally merged while this run continued; follow-up fixes are on `codex/player-attestation-release`. No API/rules deployment, signed build, TestFlight upload, or App Review submission is claimed here. Additive infrastructure changes are recorded below.

## Source and access evidence

- Initial clean branch: `codex/player-app-store-readiness`; initial/local/PR head `b713341a1d67e63d5ec3a0c301e2e044d2058ddf`.
- [PR #25](https://github.com/sahusgupta/orbit/pull/25): open, not draft, mergeable/CLEAN, no reviews or unresolved review threads. Default branch `main`; merge/squash/rebase enabled. Branch-protection REST lookup returns `403 Resource not accessible by personal access token`; this does not prove protection is absent.
- [CI run 34023254153](https://github.com/sahusgupta/orbit/actions/runs/34023254153): success on that exact initial SHA. GitGuardian also succeeds. CI logs show the full matrix, iOS export/native prebuild, Firestore emulator, and browser gates succeeded. These results do not attest later edits.
- `vercel project inspect orbit_app`: project `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`, team `sahoosegs-projects`, root `apps/api`; dashboard Node setting is currently `24.x`. Repository API configuration pins `22.x`, so the actual candidate build runtime must be verified.
- `vercel inspect https://orbitapp-one.vercel.app`: ready production deployment `dpl_CJjBqJEKrsH6sxu2mwd22m3UwWWk`, URL `https://orbit-e18gsa0uo-sahoosegs-projects.vercel.app`, created 2026-08-24. Metadata has no source SHA. Preserve this immutable deployment as the pre-change rollback target; it is not evidence of release-source compatibility.
- Initial `vercel env ls production --cwd apps/api` returned variable names/status only. Log-hash, membership-QR, deletion-pseudonym, deletion-policy, and App Check configuration variables were absent. Values were not requested. Subsequent additions are recorded below.
- Pinned Firebase CLI `projects:list`: current target `tabletalk-s` (`tabletalk-server`), project number `133175572500`. Initial `apps:list --project tabletalk-s` showed only web app `1:133175572500:web:77d0d79a654f4becfd8f01`. Subsequent iOS registration is recorded below.
- Pinned EAS `project:info --non-interactive`, from `player-app`, confirms `@saussy/tabletalk-player`, project `bb2059b7-91b3-4a6b-a66e-d5618e794fd3`. Running the same lookup from the repository root fails because that root is not an EAS project; no project was created or relinked.
- EAS existing iOS builds are old simulator/internal builds for `com.tabletalk.player`, version `0.1.0`; they cannot be uploaded as this candidate.
- EAS `credentials --platform ios`, production profile, without new Apple login: `com.orbit.player` has **No credentials set up yet**. Selecting existing App Store Connect API keys reports **There are no App Store Connect API Keys in your EAS account**. No credentials were generated or downloaded.
- Browser runtime discovery returns no available browsers. A signed-in Apple dashboard is not accessible through this session.
- EAS environment listing was rejected by automatic approval review because it can print production values. A reviewed safe alternative queried only app ID, variable names, and visibility through the existing EAS CLI authenticated GraphQL client. It succeeded: this project has no production environment overrides. No values were requested.

## Repository changes in progress

- Replace process-local quotas with atomic Firestore transactions in `orbitRateLimits`; preserve 429 responses and fail closed with 503 when shared protection cannot be evaluated.
- Use address quotas before credential verification, preventing arbitrary header rotation from creating fresh quotas. Persist only HMAC-protected references, counters, and expiry timestamps.
- Add `expiresAt` TTL configuration without a single-field index; enforce quota expiration in code independently of asynchronous TTL cleanup.
- Add concurrency, restart, expiry, transaction-retry, corrupt-state, and outage coverage; add emulator denial coverage for the server-only counter collection.
- Make the emulator test project match the existing `demo-orbit-release-ci` CLI target.

## Verification ledger for this continuation

| Command | Result |
| --- | --- |
| `npm run typecheck` before implementation | Pass: renderer, root tests, Electron, API. |
| `npx vitest run apps/api/src/http/security.test.js` before implementation | Sandbox initially denied Vite parent-directory traversal. Identical isolated rerun outside sandbox passed 7 characterization tests. |
| First durable-limiter regression run | Failed: CJS test imports used a different test-store instance, and the existing production-mode localhost route harness needed its explicit isolated datastore selection preserved. Both integration issues corrected. |
| `npx vitest run apps/api/src/http/security.test.js apps/api/src/server.routes.test.js apps/api/src/app.entry.test.js` | Pass: 3 files, 24 tests. |
| `npx vitest run apps/api/src/db/rateLimits.test.js apps/api/src/http/security.test.js apps/api/src/server.routes.test.js` | Pass: 3 files, 26 tests. |
| First `npm run typecheck:api` after editing tests | Failed on `import.meta` in CommonJS test files; replaced with `createRequire(__filename)`. Aggregate rerun subsequently passed API TypeScript. |
| First continuation `npm run verify` | Failed only the Player release contract's stale documentation assertions for the removed in-memory implementation. All other checks passed: four root TS boundaries, Player TS/artwork, Player Web TS/lint/176 tests, sales-map TS/85 tests, root/API/Player 1,282 tests (8 emulator tests skipped), and three production builds. The assertions now require the durable limiter and TTL policy; focused `npm run player:release:verify` and the subsequent full rerun pass. |
| `npm run security:paths` | Pass: no sensitive-looking project paths. |
| Final limiter `npm run verify` rerun | Pass: all 13 checks; four root TypeScript boundaries, Player TypeScript/release/artwork, Player Web TypeScript/lint/176 tests, sales-map TypeScript/85 tests, root/API/Player 1,282 passing tests (8 emulator tests skipped), and all three builds. Existing ExcelJS eval/chunk warnings remain. |
| `npm run security:dependencies` | Pass under reviewed reachability policy: root/API/Web zero production advisories; Player eight previously reviewed high build-chain exceptions, next mandatory review 2026-09-30. |
| `npm run check:independent-locks`; `npm run audit:module-graph` | Pass; no cycles, unresolved imports, dependency-boundary violations, or missing packaged runtime files. |

Local checks disable hosted sync, select unreachable loopback API origins, clear debug flags, and remove inherited credential/provider environment variables. Service inventory commands use existing CLI authentication and emit only operational metadata.

## Additive infrastructure changes

- Added `ORBIT_LOG_HASH_SECRET`, `ORBIT_MEMBERSHIP_QR_SECRET`, and `ORBIT_DELETION_PSEUDONYM_SECRET` as production **Secrets** to Vercel project `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`. Each was generated independently from 48 cryptographically random bytes and sent through stdin; no value was displayed or written locally. No overwrite flag was used. Follow-up `vercel env ls production --cwd apps/api` confirms all three names exist. The initial automatic approval rejection was resolved by presenting the user's explicit Phase 3.5/3.13 production authorization and independently verified target. Existing immutable deployment/aliases remain unchanged. Rollback before candidate promotion: remove only these three newly added variable names; do not rotate or remove existing production variables.
- Registered Firebase iOS app **Orbit Player iOS**, bundle `com.orbit.player`, app ID `1:133175572500:ios:46a75e002225158dfd8f01`, in the verified `tabletalk-s` project. Command: `node node_modules/firebase-tools/lib/bin/firebase.js apps:create IOS 'Orbit Player iOS' --bundle-id com.orbit.player --project tabletalk-s --non-interactive`. Existing web app, rules, data, and enforcement were not changed. Rollback before adoption: remove this new app registration only. App Attest registration still needs the correct Apple Team ID/signing authority.
- Public HTTPS probes: `/health`, `/privacy`, `/terms`, and `/support` return 200 without authentication. Only `/privacy` contains the exact standardized `Caminus Labs, LLC` string. These are pre-deployment observations; a content/mobile-render verification of the new legal pages remains required.
- GitHub secret-scanning alert metadata lookup returns `403 Resource not accessible by personal access token`; no alert secrets were printed. The GitGuardian success/comment is verified, but provider-side revocation is not proven by it.
- The 11 credential artifacts are present in owner-only `C:/Users/herob/AppData/Local/Orbit/SecretQuarantine/20260905-741160c`. Filename-only inspection confirms the inventory; none was opened, removed, or copied. The pilot signing key may be the only copy and must not be destroyed blindly. The directory has no standalone GitHub token file identifiable from its names.
- WSL Ubuntu exists, but `java` and Linux `node` are not in its current PATH. Local emulator/native setup is not yet reproduced; existing initial-SHA CI evidence remains distinct from final-SHA verification.

## App Check implementation and additional evidence

- Limiter commit `b71285e8ac20ddecaa8affdff2dd0a2554599917` was pushed to PR #25. [CI 34025882069](https://github.com/sahusgupta/orbit/actions/runs/34025882069) completed successfully on that exact SHA, including native generation, Firestore emulator, and browser gates. PR remains unmerged while the following changes are verified.
- Native and Web App Check acquisition is implemented, including Firebase SDK initialization order, protected API headers, phone auth, deletion, session-switch rechecks, bounded provider failures, and Web proxy/cookie refresh. Production native config requires App Check, production App Attest entitlement, and New Architecture. Detailed rollout and project-wide management compatibility gates are in [PLAYER_APP_CHECK.md](../../architecture/PLAYER_APP_CHECK.md).
- Added dedicated Android registration `1:133175572500:android:522b4cfc8de1a770fd8f01` and Player Web registration `1:133175572500:web:5e1055ff74e92d29fd8f01` to the same verified project. The original shared Web registration remains unchanged. Downloaded native client configuration directly to public configuration files; no Admin credential was downloaded. Rollback before adoption: remove only the newly added unused registrations.
- Pinned Firebase CLI initially reported App Check commands unavailable. Source inspection identified its `appcheckadmin` experiment; setting the CLI-only `FIREBASE_CLI_EXPERIMENTS=appcheckadmin` made metadata inventory available. iOS App Attest TTL is one hour, DeviceCheck/reCAPTCHA are unconfigured, and all listed Firebase service enforcement states are Off. No enforcement or debug-token change was made.
- App Check SDK 26.4.0 requires JavaScript Firebase 12.17.1; those transitive upgrades are necessary to its installation. Expo build-properties 1.0.10 introduced AJV/fast-uri high advisory entries. Updating the Player override to patched fast-uri 3.1.6 restored the unchanged advisory gate: eight pre-existing reviewed Player exceptions, no new exception added.
- An argument-free `npm install --prefix player-app --ignore-scripts` unexpectedly added `table_manager: file:..`. Release verification caught it. Removed the dependency, reran install from `player-app`, and removed only npm's leftover orphan `packages[".."]` metadata. `npm run check:independent-locks` then passed. Expo Doctor's duplicate React/EAS failures were caused by that local link; after removal, the original unchanged Doctor harness passed **18/18**. No check was suppressed.
- Existing native/API configuration tests passed 86 before extending assertions; the expanded four-file attestation/configuration run passed 99 tests. Focused Web App Check/API/proxy/cookie/auth run passed 37 tests across five files. A new native test fixture initially used a status number where its helper expected a boolean; TypeScript caught it and the fixture was corrected.
- `npm run player:config:verify` initially rejected the new entitlement under its former empty-entitlement rule. It now requires exactly production App Attest and passes. `npm run player:export:ios` produced Hermes and inspectable iOS bundles; the source scan passed (1,144 modules, 19 binary assets skipped). Export precedes the final source changes and must be rerun for final-SHA evidence.
- The first App Check `npm run verify` run passed all checks except the detected circular-local-dependency release contract. Its other results: root/Player/Web/sales-map types, Web lint, 183 Web tests, 85 sales-map tests, 1,295 root/API/Player tests with 8 emulator skips, and all three builds. A full rerun after the fix remains required.
- The next aggregate run passed every check except `scripts/legal-documents.test.js`: the new privacy disclosure had been edited in rendered HTML before its generator. Updated `scripts/import-legal-documents.cjs`, added disclosure assertions, and regenerated both API/download privacy pages with the September 6 date. Focused legal/configuration tests passed 19/19. The next aggregate rerun is pending below; no failed run is represented as a pass.
- Final native identity/config checks confirm both public service files target `tabletalk-s` and `com.orbit.player`; iOS Analytics/Ads flags are false. Expo public/introspected configuration passes with explicit New Architecture. The final native bridge's iOS Hermes/text export and source scan pass. Module graph audit passes: 236 files, 705 edges, zero cycles, unresolved imports, boundary violations, or missing packaged files.
- A names/identifiers-only query of EAS account `saussy` returns zero Apple teams (no further page). Neither signing credentials, submission keys, nor the intended Apple team identity are available in EAS. This is separate from a simulator build, which Expo supports without an Apple Developer account; simulator compilation/QA remains technically actionable.
- Added a guarded `simulator` profile inheriting production settings and the exact Xcode image, with internal simulator distribution and no remote build-number increment. It will provide pre-merge native compiler evidence on a recorded committed SHA; it is not a TestFlight artifact or signed-release evidence. The existing submission guard rejects simulator/non-store builds.
- Final App Check `npm run verify` rerun: **PASS, all 13 checks**. Four root TypeScript boundaries, Player/Web/sales-map types, Web lint, 183 Web tests, 85 sales-map tests, 1,297 root/API/Player tests with 8 separate emulator skips, and all three production builds pass. Existing ExcelJS eval/chunk warnings remain. Focused simulator/configuration/legal/TestFlight-guard run passes 23 tests. `security:paths`, independent lockfiles, module graph, and ten local documentation links also pass. Native compiler/CI evidence for the new revision remains separate.
- Player Web target inventory: Vercel `orbit-player-web`, project `prj_22SN9L7TvAKHTye7x6PZmBhvNkoq`, root `.`, dashboard Node 24.x. Existing production deployment `dpl_7A8Ptm4BcUEnkgY8gBvqKbGvj3pa`, immutable URL `https://orbit-player-kzmuv190g-sahoosegs-projects.vercel.app`; public aliases `https://orbit-player-web-liart.vercel.app` and `https://orbit-player-web-sahoosegs-projects.vercel.app`. No deployment change yet.
- reCAPTCHA Enterprise metadata lookup was initially rejected by automatic approval review for missing production authorization. Presenting the exact user Phase 3 authorization and confirmed `.firebaserc` target resolved that rejection. The approved Google API lookup then returned **403: reCAPTCHA Enterprise API has not been used or is disabled** for `tabletalk-s`. Enabling the API/provider remains actionable infrastructure work; no secret fields were queried.

## Post-merge attestation and native compiler follow-up

- PR #25 was externally squash-merged at `2026-09-06T10:02:04Z`, including limiter commit `b71285e8ac20ddecaa8affdff2dd0a2554599917`. The merged source is `5b5b18ec92ed94feb125eb8afd0a0140fdfb4ff8`. This agent did not perform that merge. The limiter's [CI 34025882069](https://github.com/sahusgupta/orbit/actions/runs/34025882069) succeeded.
- App Check commit `2ec17ef3acd45dd57ff0893c6a0f77809d3a8afa` was committed and pushed to the former PR branch after the external merge. Automatic approval initially rejected the push; the exact Phase 1.6 and Phase 2 user authorization and verified remote/branch resolved the rejection. No new PR check triggered because PR #25 was closed. The same change was cherry-picked onto a new branch from the merged source as `f05fe3c`; the original branch/history is preserved.
- [EAS simulator build 031f69ba-009b-44c4-b251-dbfea73dd117](https://expo.dev/accounts/saussy/projects/tabletalk-player/builds/031f69ba-009b-44c4-b251-dbfea73dd117), source `2ec17ef3acd45dd57ff0893c6a0f77809d3a8afa`, version `1.0.0`, build `1`, failed during CocoaPods code generation with `TypeError: expand is not a function`. No simulator artifact or signed build was produced. Sanitized logs identified the existing blanket `brace-expansion@5.0.9` override as incompatible with React Native's minimatch 3 CommonJS caller.
- Replaced only the Player blanket override with patched parent-compatible lines `1.1.18`, `2.1.4`, and `5.0.9`. The upstream [GHSA-rgw5-rvv9-x895 advisory](https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895) lists all three as patched. `npm run player:codegen:ios` now runs the actual React Native iOS schema/provider generator, verifies required generated artifacts, and removes its isolated output. It passes locally and is added to CI; it does not replace CocoaPods/Xcode compilation.
- Enabled only `recaptchaenterprise.googleapis.com` in project `133175572500` after recording `DISABLED`. Service Usage returned `ENABLED`; key creation initially returned the documented propagation 403, then succeeded on retry. Created public SCORE key `projects/133175572500/keys/6LcsaawtAAAAABW9SPwprtLXkOdd-C_Ganc5MYtm`, restricted to `orbit-player-web-liart.vercel.app` and `orbit-player-web-sahoosegs-projects.vercel.app` (`allowAllDomains=false`). Existing key inventory was empty.
- Configured dedicated Web app `1:133175572500:web:5e1055ff74e92d29fd8f01` with this Enterprise provider, minimum score `0.5`, token TTL `1h`. Follow-up provider inventory confirms the settings, and every Firebase enforcement service remains Off/never updated. No debug provider/token was created. Before adoption, rollback is to remove this new unused Web provider/key, then disable only the newly enabled reCAPTCHA service. Do not remove the pre-existing management Web app.
- The first post-cherry-pick full verification found a Windows checkout issue: `core.autocrlf=true` converted the generated HTML to CRLF, breaking the strict deterministic legal-copy comparison. Added LF attributes only for the six generated legal files and regenerated them; no test was relaxed.
- Backed up the deployed Firestore release `projects/tabletalk-s/rulesets/a74cb1f9-275a-491c-a8f7-0c513241cade` (updated 2026-08-12), its rules source (SHA-256 `0c1805ef988d2ea68913b65524009698bfa1f5c236785cce1779ef52cc474f21`), 15 indexes, and 3 field overrides. The ignored `out/release-20260906/firebase-security-before.json` contains configuration only, no documents/credentials. Initial index inventory rejected explicit page size 100; retry with the supported default succeeded. No Firestore deployment yet.
- [Follow-up PR #26](https://github.com/sahusgupta/orbit/pull/26) contains the attestation and compiler fixes. CLI PR creation returned `Resource not accessible by personal access token`; the existing connected GitHub account created the PR successfully. Commit `d9faf19fe5466b63dd7363dd932cf5d0f8c362ce` passes all 13 local `npm run verify` checks, with 1,297 root/API/Player tests, 183 Web tests, 85 sales-map tests, and the unchanged eight emulator skips. Exact-head [CI 34028552437](https://github.com/sahusgupta/orbit/actions/runs/34028552437) was still running at this observation.
- [Second simulator build e003f9d9-2f52-4fdc-a99c-505db6c45748](https://expo.dev/accounts/saussy/projects/tabletalk-player/builds/e003f9d9-2f52-4fdc-a99c-505db6c45748), source `d9faf19fe5466b63dd7363dd932cf5d0f8c362ce`, passed CocoaPods installation (96 pods) but failed in EAS `export:embed`: Metro injects `EXPO_PUBLIC_PROJECT_ROOT`, which the production allowlist rejected. The corrected guard permits this SDK metadata only when it resolves to the actual Player directory; arbitrary public variables and other directory values still fail closed. This attempt produced no app artifact.
- Provisioned and verified seven production settings on the existing Player Web project: `ORBIT_API_URL`, `NEXT_PUBLIC_ORBIT_API_URL`, `NEXT_PUBLIC_PLAYER_WEB_URL`, `NEXT_PUBLIC_ENABLE_FIREBASE_SYNC`, `NEXT_PUBLIC_APP_CHECK_ENABLED`, `NEXT_PUBLIC_APP_CHECK_SITE_KEY`, and `NEXT_PUBLIC_FIREBASE_APP_ID`. Existing variable inventory was empty. Vercel required explicit `--type config` for the intentionally public site key; no provider secret was exposed. Existing deployment remains unchanged. Before adoption, remove only these seven new variables to roll back configuration.
- IAM key-metadata lookup matched quarantined filename prefix `bffbadd088` to enabled key `bffbadd088e4b1f7b93dd7fa9cfb58c49cc2a259` on `firebase-adminsdk-fbsvc@tabletalk-s.iam.gserviceaccount.com`. A second enabled key `fb29d68eaf68bef1fe0dcbf58a8862721d3a503e` exists. No private key was read or changed. Establish production/automation usage before rotating or disabling either; this is actionable credential cleanup, not completed revocation evidence.
- Metro correction verification: 19 configuration/environment tests pass; production Expo config/permissions pass; `npm run player:export:ios` passes Hermes, inspectable JavaScript, and the newly included actual EAS `export:embed` path (1,144 modules, 19 assets). Both JavaScript scans pass. The full verification rerun is in progress on this correction.
- Automatic approval review rejected Vercel `env run` for a prepared metadata-only Firebase credential check because it would load the production credential into a local process. The operation did not execute. Explicit user permission is pending for this exact access; do not retry or rotate the possibly active key until that permission or equivalent owner-supplied usage evidence arrives.
- Commit `dab4aa7183b6c4482310feb9b1c4f9df3bfcda41` passes all 13 local `npm run verify` checks: 1,298 root/API/Player tests, 183 Web tests, 85 sales-map tests, and the unchanged eight separate emulator skips. Prior commit `d9faf19` also passed its complete CI run. Exact `dab4aa7` CI is [34028934372](https://github.com/sahusgupta/orbit/actions/runs/34028934372).
- [Third simulator build cc64f693-fdf4-416c-8fdc-c2fcc3b9cd99](https://expo.dev/accounts/saussy/projects/tabletalk-player/builds/cc64f693-fdf4-416c-8fdc-c2fcc3b9cd99), source `dab4aa7183b6c4482310feb9b1c4f9df3bfcda41`, version `1.0.0` build `1`, passed CocoaPods and embedded JavaScript bundling; native compilation is still running at this observation. Added a [manual simulator QA workflow](../../../player-app/.eas/workflows/player-simulator-qa.yml) and [bounded local-profile flow](../../../player-app/.maestro/README.md). The automation has not yet run; it is not passing QA evidence.

## Hosted runtime and simulator verification follow-up

- `dab4aa7` CI [34028934372](https://github.com/sahusgupta/orbit/actions/runs/34028934372) succeeded. Simulator automation commit `2da2c6fc57acc69df9ae0cefbd2dd30e97a5f1a9` was committed/pushed to PR #26; its general [CI 34029362453](https://github.com/sahusgupta/orbit/actions/runs/34029362453) succeeded. The [macOS simulator job 34029362548](https://github.com/sahusgupta/orbit/actions/runs/34029362548) installed dependencies/Maestro and is still running its compiler/QA step at this observation.
- EAS simulator build `cc64f693-fdf4-416c-8fdc-c2fcc3b9cd99` **finished successfully**. Downloaded its 19,422,049-byte archive through the existing EAS session without disclosing its artifact URL. SHA-256: `0e16b22ae0e4bc09bb443b904b7320c90e0a1ab78595304b1d45ecb74b88767c`. WSL Python inspected its binary plists directly from the archive after Windows Python execution was denied. The binary matches `com.orbit.player`, version `1.0.0` build `1`, iPhone-only, minimum iOS `15.1`, SDK `26.0` (`23A339`), Xcode `17A324`, and the dedicated Firebase project/app. Camera is its only usage-description permission; there is no URL scheme, Google Maps bundle, Analytics enablement, or tracking declaration. Fifteen bundled privacy manifests were inspected. [The privacy audit](../../../player-app/PRIVACY_MANIFEST_AUDIT.md) records SDK aggregation and the remaining signed-archive limitation.
- Four existing API characterization files passed 30 tests before changes. New regression cases reproduced hosted-development raw error logging and localhost CORS acceptance. An initial telemetry regression fixture correctly hit the production ban on an in-memory store; it was replaced by a real serializer test with transport doubles, which reproduced the raw-stack problem without changing the database guard. Hosted error logs/storage, CORS/HSTS, dashboard cookies, and check-in-origin fallback now apply the existing hosted-or-production boundary. Focused runtime/version/retention tests passed 37; dashboard/session checks passed 28 before their fix and 7 afterward; check-in-origin tests passed 23 afterward.
- Added `GET /version` with validated full source SHA, uncached responses, and honest 503 when deployment metadata is missing. Operational telemetry/error/update records now receive private server-derived 30-day `expiresAt` timestamps, with corresponding TTL/index-exemption deployment inputs; response shapes remain unchanged. No provider TTL or historical backfill has been applied yet.
- Hosted Player Web configuration now rejects missing production identity/URLs, disabled App Check/sync, emulators, and unreviewed public environment overrides; 21 focused tests pass. Deployment staging preserves this guard and excludes environment files, Git metadata, and generated evidence; both staging tests pass. The existing staging script already produces a self-contained Web artifact, so the provider's root `.` can remain unchanged; do not change it to `player-web` when deploying that staged artifact.
- The first aggregate runtime-fix check failed only API check-JS because the new CommonJS test used `import.meta`. Corrected it to the repository's `createRequire(__filename)` pattern. The next `npm run verify` passed all 13 checks, including 1,305 root/API/Player tests, 204 Web tests, and 85 sales-map tests (eight separate emulator skips). After cookie/origin/staging additions, the final `npm run verify` again passed all 13 checks: 1,308 root/API/Player tests, 204 Web tests, and 85 sales-map tests, with the unchanged eight separate emulator skips. Existing ExcelJS eval and bundle-size warnings remain.
- Created separate Firebase browser client key resource `projects/133175572500/locations/global/keys/orbit-player-web-production`, restricted to the two verified Player Web domain referrers and only Identity Toolkit, Secure Token, Firestore, Firebase Installations, and Firebase App Check APIs. Existing management/native keys are unchanged. Follow-up metadata confirms restrictions. Invalid synthetic-token lookup probes prove the allowed referrer reaches Auth (400 `INVALID_ID_TOKEN`), while disallowed/missing referrers return 403 `API_KEY_HTTP_REFERRER_BLOCKED`; no account was created or read.
- Added five more public Vercel Web production settings: `NEXT_PUBLIC_FIREBASE_API_KEY`, `NEXT_PUBLIC_FIREBASE_PROJECT_ID`, `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`, `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`, and `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`. The initial approval rejection was resolved by rereading exact user Phase 3.12/3.13 and re-verifying project identity/absent names. The browser-visible key moved directly through process memory/stdin, with no local key file or printed value. Rollback before adoption removes only these five additions and the unused dedicated key; there is still no Web deployment change.
- Backed up Firebase Auth domain/provider metadata. Email/password is enabled; its initial domains were `localhost`, `tabletalk-s.firebaseapp.com`, and `tabletalk-s.web.app`. Added only `orbit-player-web-liart.vercel.app` and `orbit-player-web-sahoosegs-projects.vercel.app`, rechecked the unchanged initial list before patching, and verified the exact five-domain result afterward. No provider/account/email-delivery change. Rollback removes only the two new domains.
- Production API CORS preflight was rejected twice by automatic approval review, including after the explicit production-smoke request was reread. The second rejection called attachment-derived authorization untrusted. No request executed. A separate user approval question is pending for three `OPTIONS https://orbitapp-one.vercel.app/player/discovery` requests using the two verified Web origins and `https://untrusted-release-probe.invalid`; no credentials or player data would be sent. Do not retry this operation through another tool while approval remains pending.

## Integrity and rendered-QA follow-up

- Before further publisher changes, `npm run verify` passed all 13 checks with 1,313 root/API/Player tests, 204 Web tests, 85 sales-map tests, and eight separate emulator skips. The later Firestore acknowledgement fix requires a new aggregate run.
- [The security review](../reviews/player-release-security-20260906.md) records four reproduced and fixed defects: Firestore duplicate-receipt transaction ordering, deletion cleanup removing a concurrent newer revision, QR responses crossing a changed staff session, and HTTP-200 Firestore batches containing rejected writes. Each has a regression that failed before its fix. Publisher orchestration tests prove the parent commit marker is withheld after failed projection or cleanup writes.
- iOS v1 now exposes email/password authentication only, because production SMS delivery is not provisioned. Existing phone-authenticated account compatibility remains intact. Onboarding includes explicit VoiceOver labels and disabled/checked states; rendered/authentication tests pass. Updated the four generated privacy/support pages from their source and reviewed the intentional component-digest updates; unchanged style digests continue to pass.
- `npm run player:export:ios` passed after the native email/VoiceOver changes: 1,145 modules, 19 assets, Hermes approximately 5.35 MB and inspectable JavaScript approximately 3.78 MB; both bundle scans and actual `export:embed` passed. That export came from the then-uncommitted follow-up tree, not the older completed EAS simulator artifact.
- `npm run e2e:public` passed eight public routes at desktop/mobile sizes without console failures or horizontal overflow. Its local evidence directory is the OS temporary `orbit-public-site-smoke` directory; the run does not verify a new production legal deployment.
- The first `npm run web:e2e` completed 90 captures/42 interactions but failed five cold homepage content checks. A second run passed all existing assertions but failed two new no-JavaScript assertions because Next's streamed page remains hidden without its reveal script. The hero now has immediately readable server-rendered text/actions, covered by a real server-render test; the outer layout provides a readable no-JavaScript explanation and a static Privacy Policy link. The harness waits for readable content/fonts without relaxing existing content, metadata, layout, or console checks. The final rerun **passed 15 routes, six viewports, 90 captures, and 44 interactions**. Sanitized evidence is in ignored `test-results/player-web/screenshots/`; logs are in ignored `out/release-20260906/`.
- `npm run security:paths`, `npm run check:release-controls`, and `npm run audit:module-graph` pass after these fixes. The graph covers 238 modules/709 relative edges with zero cycles, boundary violations, unresolved imports, or missing packaged runtime files.
- The first aggregate rerun failed only API TypeScript on an incomplete new `crypto.Sign` test double. Replaced that double with a real Sign object whose signing method is stubbed; `npm run typecheck:api` and all 36 publisher tests passed. Final `npm run verify` then **passed all 13 checks**, including 1,328 root/API/Player tests, 205 Web tests, and 85 sales-map tests; eight separate emulator tests remain skipped locally. Existing ExcelJS eval/chunk warnings remain.
- Committed integrity fixes as `b02be44c39c3bfe84de3351057590571bae0ec20` and native/Web/QA fixes as `d9fda7e5a001b96a0a7ac68f4b18fb578536d191`. Reviewed unstaged/staged diffs and ran Gitleaks on each staged patch: 13.57 KB and 16.12 KB scanned, zero findings. These commits are still local while the previous long-running native job is preserved; pushing would cancel that job.
- Added the durable [release manifest and artifact index](../../../player-app/submission/2026-09-06/README.md), distinguishing the current code SHA, older completed EAS simulator artifact, provider rollback targets, actual checks, and unresolved authority/configuration fields.
- The next simulator run targets only the runner's native simulator architecture, reports command elapsed times, and writes compiler output continuously to evidence files. This improves diagnosis if CI cancels or times out. The already-running source `2da2c6f` job still uses the previous script; no success is claimed for that job yet.

## Human gates evidenced so far (still pending)

- Subsequent production Firebase API-key metadata inventory was rejected by automatic approval review, which did not accept attachment-derived authorization. A user question is pending for key resource names/restrictions only in `tabletalk-s` / `133175572500`; no key strings are requested and no rejected inventory was rerouted. Existing native API allowlists remain unchanged.

- EAS `workflow:validate .eas/workflows/player-simulator-qa.yml --non-interactive` returned `Running maestro_test jobs requires a paid plan` (request `966e5cf8-f2d0-4527-8e7f-49cc7b8f60ad`). No plan was purchased and no QA job ran there. Added a GitHub-hosted macOS Release compiler/simulator workflow as the actionable alternative, with no signing or production credentials. Its results remain pending.

- Apple Account Holder: authenticate the intended Caminus Labs, LLC team and configure existing/new signing and submission credentials for `com.orbit.player`. EAS currently has neither. This blocks signed iOS/TestFlight work; no password or MFA code should enter chat or repository files.
- Qualified counsel/venue owner: supply existing approved territorial/venue authority and retention decisions. The repository's example deletion policy is explicitly not approval. Technical preparation continues while these decisions are located.

Remaining deployment/configuration verification, native QA execution, credential cleanup after approved access, and final artifact reconciliation remain engineering work, not completed human-only gates. App Check source integration/provider preparation and native QA automation are implemented; enforcement and successful interaction evidence remain separate requirements.
