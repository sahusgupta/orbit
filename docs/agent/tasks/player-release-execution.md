# Orbit Player release execution — 2026-09-06

This is the active execution record for the authorized continuation of [Player App Store readiness](player-app-store-readiness.md). It supersedes that task's earlier restriction against deployment/build work for this run only. Work is ongoing. PR #25 was externally merged while this run continued; follow-up fixes are on `codex/player-attestation-release`. The approved API candidate was created and rolled back as recorded at the end of this ledger; a later explicitly approved candidate is now retained and build-verified as recorded below. Rules deployment, signed build, TestFlight upload, and App Review submission remain incomplete. Additive infrastructure changes are recorded below.

## Source and access evidence

- Initial clean branch: `codex/player-app-store-readiness`; initial/local/PR head `b713341a1d67e63d5ec3a0c301e2e044d2058ddf`.
- [PR #25](https://github.com/sahusgupta/orbit/pull/25): open, not draft, mergeable/CLEAN, no reviews or unresolved review threads. Default branch `main`; merge/squash/rebase enabled. Branch-protection REST lookup returns `403 Resource not accessible by personal access token`; this does not prove protection is absent.
- [CI run 34023254153](https://github.com/sahusgupta/orbit/actions/runs/34023254153): success on that exact initial SHA. GitGuardian also succeeds. CI logs show the full matrix, iOS export/native prebuild, Firestore emulator, and browser gates succeeded. These results do not attest later edits.
- `vercel project inspect orbit_app`: project `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`, team `sahoosegs-projects`, root `apps/api`; dashboard Node setting is currently `24.x`. Repository API configuration pins `22.x`, so the actual candidate build runtime must be verified.
- `vercel inspect https://orbitapp-one.vercel.app`: ready production deployment `dpl_CJjBqJEKrsH6sxu2mwd22m3UwWWk`, URL `https://orbit-e18gsa0uo-sahoosegs-projects.vercel.app`, created 2026-08-24. Metadata has no source SHA. Preserve this immutable deployment as the pre-change rollback target; it is not evidence of release-source compatibility.
- Initial `vercel env ls production --cwd apps/api` returned variable names/status only. Log-hash, membership-QR, deletion-pseudonym, deletion-policy, and App Check configuration variables were absent. Values were not requested. Subsequent additions are recorded below.
- Pinned Firebase CLI `projects:list`: current target `tabletalk-s` (`tabletalk-server`), project number `133175572500`. Initial `apps:list --project tabletalk-s` showed only web app `1:133175572500:web:77d0d79a654f4becfd8f01`. Subsequent iOS registration is recorded below.
- Pinned EAS `project:info --non-interactive`, from `player-app`, confirms `@saussy/tabletalk-player`, project `bb2059b7-91b3-4a6b-a66e-d5618e794fd3`. Running the same lookup from the repository root fails because that root is not an EAS project; no project was created or relinked.
- EAS existing iOS builds are old simulator/internal builds for `com.tabletalk.player`, version `0.1.0`; they cannot be uploaded as this candidate.
- EAS `credentials --platform ios`, production profile, without new Apple login: `com.orbit.player` has **No credentials set up yet**. Selecting existing App Store Connect API keys reports **There are no App Store Connect API Keys in your EAS account**. No credentials were generated or downloaded.
- Browser runtime discovery returns no available browsers. A signed-in Apple dashboard is not accessible through this session.
- EAS environment listing was rejected by automatic approval review because it can print production values. A reviewed safe alternative queried only app ID, variable names, and visibility through the existing EAS CLI authenticated GraphQL client. It succeeded: this project has no production environment overrides. No values were requested.

## Repository changes in progress

- Replace process-local quotas with atomic Firestore transactions in `orbitRateLimits`; preserve 429 responses and fail closed with 503 when shared protection cannot be evaluated.
- Use address quotas before credential verification, preventing arbitrary header rotation from creating fresh quotas. Persist only HMAC-protected references, counters, and expiry timestamps.
- Add `expiresAt` TTL configuration without a single-field index; enforce quota expiration in code independently of asynchronous TTL cleanup.
- Add concurrency, restart, expiry, transaction-retry, corrupt-state, and outage coverage; add emulator denial coverage for the server-only counter collection.
- Make the emulator test project match the existing `demo-orbit-release-ci` CLI target.

## Verification ledger for this continuation

| Command | Result |
| --- | --- |
| `npm run typecheck` before implementation | Pass: renderer, root tests, Electron, API. |
| `npx vitest run apps/api/src/http/security.test.js` before implementation | Sandbox initially denied Vite parent-directory traversal. Identical isolated rerun outside sandbox passed 7 characterization tests. |
| First durable-limiter regression run | Failed: CJS test imports used a different test-store instance, and the existing production-mode localhost route harness needed its explicit isolated datastore selection preserved. Both integration issues corrected. |
| `npx vitest run apps/api/src/http/security.test.js apps/api/src/server.routes.test.js apps/api/src/app.entry.test.js` | Pass: 3 files, 24 tests. |
| `npx vitest run apps/api/src/db/rateLimits.test.js apps/api/src/http/security.test.js apps/api/src/server.routes.test.js` | Pass: 3 files, 26 tests. |
| First `npm run typecheck:api` after editing tests | Failed on `import.meta` in CommonJS test files; replaced with `createRequire(__filename)`. Aggregate rerun subsequently passed API TypeScript. |
| First continuation `npm run verify` | Failed only the Player release contract's stale documentation assertions for the removed in-memory implementation. All other checks passed: four root TS boundaries, Player TS/artwork, Player Web TS/lint/176 tests, sales-map TS/85 tests, root/API/Player 1,282 tests (8 emulator tests skipped), and three production builds. The assertions now require the durable limiter and TTL policy; focused `npm run player:release:verify` and the subsequent full rerun pass. |
| `npm run security:paths` | Pass: no sensitive-looking project paths. |
| Final limiter `npm run verify` rerun | Pass: all 13 checks; four root TypeScript boundaries, Player TypeScript/release/artwork, Player Web TypeScript/lint/176 tests, sales-map TypeScript/85 tests, root/API/Player 1,282 passing tests (8 emulator tests skipped), and all three builds. Existing ExcelJS eval/chunk warnings remain. |
| `npm run security:dependencies` | Pass under reviewed reachability policy: root/API/Web zero production advisories; Player eight previously reviewed high build-chain exceptions, next mandatory review 2026-09-30. |
| `npm run check:independent-locks`; `npm run audit:module-graph` | Pass; no cycles, unresolved imports, dependency-boundary violations, or missing packaged runtime files. |

Local checks disable hosted sync, select unreachable loopback API origins, clear debug flags, and remove inherited credential/provider environment variables. Service inventory commands use existing CLI authentication and emit only operational metadata.

## Additive infrastructure changes

- Added `ORBIT_LOG_HASH_SECRET`, `ORBIT_MEMBERSHIP_QR_SECRET`, and `ORBIT_DELETION_PSEUDONYM_SECRET` as production **Secrets** to Vercel project `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`. Each was generated independently from 48 cryptographically random bytes and sent through stdin; no value was displayed or written locally. No overwrite flag was used. Follow-up `vercel env ls production --cwd apps/api` confirms all three names exist. The initial automatic approval rejection was resolved by presenting the user's explicit Phase 3.5/3.13 production authorization and independently verified target. Existing immutable deployment/aliases remain unchanged. Rollback before candidate promotion: remove only these three newly added variable names; do not rotate or remove existing production variables.
- Registered Firebase iOS app **Orbit Player iOS**, bundle `com.orbit.player`, app ID `1:133175572500:ios:46a75e002225158dfd8f01`, in the verified `tabletalk-s` project. Command: `node node_modules/firebase-tools/lib/bin/firebase.js apps:create IOS 'Orbit Player iOS' --bundle-id com.orbit.player --project tabletalk-s --non-interactive`. Existing web app, rules, data, and enforcement were not changed. Rollback before adoption: remove this new app registration only. App Attest registration still needs the correct Apple Team ID/signing authority.
- Public HTTPS probes: `/health`, `/privacy`, `/terms`, and `/support` return 200 without authentication. Only `/privacy` contains the exact standardized `Caminus Labs, LLC` string. These are pre-deployment observations; a content/mobile-render verification of the new legal pages remains required.
- GitHub secret-scanning alert metadata lookup returns `403 Resource not accessible by personal access token`; no alert secrets were printed. The GitGuardian success/comment is verified, but provider-side revocation is not proven by it.
- The 11 credential artifacts are present in owner-only `C:/Users/herob/AppData/Local/Orbit/SecretQuarantine/20260905-741160c`. Filename-only inspection confirms the inventory; none was opened, removed, or copied. The pilot signing key may be the only copy and must not be destroyed blindly. The directory has no standalone GitHub token file identifiable from its names.
- WSL Ubuntu exists, but `java` and Linux `node` are not in its current PATH. Local emulator/native setup is not yet reproduced; existing initial-SHA CI evidence remains distinct from final-SHA verification.

## App Check implementation and additional evidence

- Limiter commit `b71285e8ac20ddecaa8affdff2dd0a2554599917` was pushed to PR #25. [CI 34025882069](https://github.com/sahusgupta/orbit/actions/runs/34025882069) completed successfully on that exact SHA, including native generation, Firestore emulator, and browser gates. PR remains unmerged while the following changes are verified.
- Native and Web App Check acquisition is implemented, including Firebase SDK initialization order, protected API headers, phone auth, deletion, session-switch rechecks, bounded provider failures, and Web proxy/cookie refresh. Production native config requires App Check, production App Attest entitlement, and New Architecture. Detailed rollout and project-wide management compatibility gates are in [PLAYER_APP_CHECK.md](../../architecture/PLAYER_APP_CHECK.md).
- Added dedicated Android registration `1:133175572500:android:522b4cfc8de1a770fd8f01` and Player Web registration `1:133175572500:web:5e1055ff74e92d29fd8f01` to the same verified project. The original shared Web registration remains unchanged. Downloaded native client configuration directly to public configuration files; no Admin credential was downloaded. Rollback before adoption: remove only the newly added unused registrations.
- Pinned Firebase CLI initially reported App Check commands unavailable. Source inspection identified its `appcheckadmin` experiment; setting the CLI-only `FIREBASE_CLI_EXPERIMENTS=appcheckadmin` made metadata inventory available. iOS App Attest TTL is one hour, DeviceCheck/reCAPTCHA are unconfigured, and all listed Firebase service enforcement states are Off. No enforcement or debug-token change was made.
- App Check SDK 26.4.0 requires JavaScript Firebase 12.17.1; those transitive upgrades are necessary to its installation. Expo build-properties 1.0.10 introduced AJV/fast-uri high advisory entries. Updating the Player override to patched fast-uri 3.1.6 restored the unchanged advisory gate: eight pre-existing reviewed Player exceptions, no new exception added.
- An argument-free `npm install --prefix player-app --ignore-scripts` unexpectedly added `table_manager: file:..`. Release verification caught it. Removed the dependency, reran install from `player-app`, and removed only npm's leftover orphan `packages[".."]` metadata. `npm run check:independent-locks` then passed. Expo Doctor's duplicate React/EAS failures were caused by that local link; after removal, the original unchanged Doctor harness passed **18/18**. No check was suppressed.
- Existing native/API configuration tests passed 86 before extending assertions; the expanded four-file attestation/configuration run passed 99 tests. Focused Web App Check/API/proxy/cookie/auth run passed 37 tests across five files. A new native test fixture initially used a status number where its helper expected a boolean; TypeScript caught it and the fixture was corrected.
- `npm run player:config:verify` initially rejected the new entitlement under its former empty-entitlement rule. It now requires exactly production App Attest and passes. `npm run player:export:ios` produced Hermes and inspectable iOS bundles; the source scan passed (1,144 modules, 19 binary assets skipped). Export precedes the final source changes and must be rerun for final-SHA evidence.
- The first App Check `npm run verify` run passed all checks except the detected circular-local-dependency release contract. Its other results: root/Player/Web/sales-map types, Web lint, 183 Web tests, 85 sales-map tests, 1,295 root/API/Player tests with 8 emulator skips, and all three builds. A full rerun after the fix remains required.
- The next aggregate run passed every check except `scripts/legal-documents.test.js`: the new privacy disclosure had been edited in rendered HTML before its generator. Updated `scripts/import-legal-documents.cjs`, added disclosure assertions, and regenerated both API/download privacy pages with the September 6 date. Focused legal/configuration tests passed 19/19. The next aggregate rerun is pending below; no failed run is represented as a pass.
- Final native identity/config checks confirm both public service files target `tabletalk-s` and `com.orbit.player`; iOS Analytics/Ads flags are false. Expo public/introspected configuration passes with explicit New Architecture. The final native bridge's iOS Hermes/text export and source scan pass. Module graph audit passes: 236 files, 705 edges, zero cycles, unresolved imports, boundary violations, or missing packaged files.
- A names/identifiers-only query of EAS account `saussy` returns zero Apple teams (no further page). Neither signing credentials, submission keys, nor the intended Apple team identity are available in EAS. This is separate from a simulator build, which Expo supports without an Apple Developer account; simulator compilation/QA remains technically actionable.
- Added a guarded `simulator` profile inheriting production settings and the exact Xcode image, with internal simulator distribution and no remote build-number increment. It will provide pre-merge native compiler evidence on a recorded committed SHA; it is not a TestFlight artifact or signed-release evidence. The existing submission guard rejects simulator/non-store builds.
- Final App Check `npm run verify` rerun: **PASS, all 13 checks**. Four root TypeScript boundaries, Player/Web/sales-map types, Web lint, 183 Web tests, 85 sales-map tests, 1,297 root/API/Player tests with 8 separate emulator skips, and all three production builds pass. Existing ExcelJS eval/chunk warnings remain. Focused simulator/configuration/legal/TestFlight-guard run passes 23 tests. `security:paths`, independent lockfiles, module graph, and ten local documentation links also pass. Native compiler/CI evidence for the new revision remains separate.
- Player Web target inventory: Vercel `orbit-player-web`, project `prj_22SN9L7TvAKHTye7x6PZmBhvNkoq`, root `.`, dashboard Node 24.x. Existing production deployment `dpl_7A8Ptm4BcUEnkgY8gBvqKbGvj3pa`, immutable URL `https://orbit-player-kzmuv190g-sahoosegs-projects.vercel.app`; public aliases `https://orbit-player-web-liart.vercel.app` and `https://orbit-player-web-sahoosegs-projects.vercel.app`. No deployment change yet.
- reCAPTCHA Enterprise metadata lookup was initially rejected by automatic approval review for missing production authorization. Presenting the exact user Phase 3 authorization and confirmed `.firebaserc` target resolved that rejection. The approved Google API lookup then returned **403: reCAPTCHA Enterprise API has not been used or is disabled** for `tabletalk-s`. Enabling the API/provider remains actionable infrastructure work; no secret fields were queried.

## Post-merge attestation and native compiler follow-up

- PR #25 was externally squash-merged at `2026-09-06T10:02:04Z`, including limiter commit `b71285e8ac20ddecaa8affdff2dd0a2554599917`. The merged source is `5b5b18ec92ed94feb125eb8afd0a0140fdfb4ff8`. This agent did not perform that merge. The limiter's [CI 34025882069](https://github.com/sahusgupta/orbit/actions/runs/34025882069) succeeded.
- App Check commit `2ec17ef3acd45dd57ff0893c6a0f77809d3a8afa` was committed and pushed to the former PR branch after the external merge. Automatic approval initially rejected the push; the exact Phase 1.6 and Phase 2 user authorization and verified remote/branch resolved the rejection. No new PR check triggered because PR #25 was closed. The same change was cherry-picked onto a new branch from the merged source as `f05fe3c`; the original branch/history is preserved.
- [EAS simulator build 031f69ba-009b-44c4-b251-dbfea73dd117](https://expo.dev/accounts/saussy/projects/tabletalk-player/builds/031f69ba-009b-44c4-b251-dbfea73dd117), source `2ec17ef3acd45dd57ff0893c6a0f77809d3a8afa`, version `1.0.0`, build `1`, failed during CocoaPods code generation with `TypeError: expand is not a function`. No simulator artifact or signed build was produced. Sanitized logs identified the existing blanket `brace-expansion@5.0.9` override as incompatible with React Native's minimatch 3 CommonJS caller.
- Replaced only the Player blanket override with patched parent-compatible lines `1.1.18`, `2.1.4`, and `5.0.9`. The upstream [GHSA-rgw5-rvv9-x895 advisory](https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895) lists all three as patched. `npm run player:codegen:ios` now runs the actual React Native iOS schema/provider generator, verifies required generated artifacts, and removes its isolated output. It passes locally and is added to CI; it does not replace CocoaPods/Xcode compilation.
- Enabled only `recaptchaenterprise.googleapis.com` in project `133175572500` after recording `DISABLED`. Service Usage returned `ENABLED`; key creation initially returned the documented propagation 403, then succeeded on retry. Created public SCORE key `projects/133175572500/keys/6LcsaawtAAAAABW9SPwprtLXkOdd-C_Ganc5MYtm`, restricted to `orbit-player-web-liart.vercel.app` and `orbit-player-web-sahoosegs-projects.vercel.app` (`allowAllDomains=false`). Existing key inventory was empty.
- Configured dedicated Web app `1:133175572500:web:5e1055ff74e92d29fd8f01` with this Enterprise provider, minimum score `0.5`, token TTL `1h`. Follow-up provider inventory confirms the settings, and every Firebase enforcement service remains Off/never updated. No debug provider/token was created. Before adoption, rollback is to remove this new unused Web provider/key, then disable only the newly enabled reCAPTCHA service. Do not remove the pre-existing management Web app.
- The first post-cherry-pick full verification found a Windows checkout issue: `core.autocrlf=true` converted the generated HTML to CRLF, breaking the strict deterministic legal-copy comparison. Added LF attributes only for the six generated legal files and regenerated them; no test was relaxed.
- Backed up the deployed Firestore release `projects/tabletalk-s/rulesets/a74cb1f9-275a-491c-a8f7-0c513241cade` (updated 2026-08-12), its rules source (SHA-256 `0c1805ef988d2ea68913b65524009698bfa1f5c236785cce1779ef52cc474f21`), 15 indexes, and 3 field overrides. The ignored `out/release-20260906/firebase-security-before.json` contains configuration only, no documents/credentials. Initial index inventory rejected explicit page size 100; retry with the supported default succeeded. No Firestore deployment yet.
- [Follow-up PR #26](https://github.com/sahusgupta/orbit/pull/26) contains the attestation and compiler fixes. CLI PR creation returned `Resource not accessible by personal access token`; the existing connected GitHub account created the PR successfully. Commit `d9faf19fe5466b63dd7363dd932cf5d0f8c362ce` passes all 13 local `npm run verify` checks, with 1,297 root/API/Player tests, 183 Web tests, 85 sales-map tests, and the unchanged eight emulator skips. Exact-head [CI 34028552437](https://github.com/sahusgupta/orbit/actions/runs/34028552437) was still running at this observation.
- [Second simulator build e003f9d9-2f52-4fdc-a99c-505db6c45748](https://expo.dev/accounts/saussy/projects/tabletalk-player/builds/e003f9d9-2f52-4fdc-a99c-505db6c45748), source `d9faf19fe5466b63dd7363dd932cf5d0f8c362ce`, passed CocoaPods installation (96 pods) but failed in EAS `export:embed`: Metro injects `EXPO_PUBLIC_PROJECT_ROOT`, which the production allowlist rejected. The corrected guard permits this SDK metadata only when it resolves to the actual Player directory; arbitrary public variables and other directory values still fail closed. This attempt produced no app artifact.
- Provisioned and verified seven production settings on the existing Player Web project: `ORBIT_API_URL`, `NEXT_PUBLIC_ORBIT_API_URL`, `NEXT_PUBLIC_PLAYER_WEB_URL`, `NEXT_PUBLIC_ENABLE_FIREBASE_SYNC`, `NEXT_PUBLIC_APP_CHECK_ENABLED`, `NEXT_PUBLIC_APP_CHECK_SITE_KEY`, and `NEXT_PUBLIC_FIREBASE_APP_ID`. Existing variable inventory was empty. Vercel required explicit `--type config` for the intentionally public site key; no provider secret was exposed. Existing deployment remains unchanged. Before adoption, remove only these seven new variables to roll back configuration.
- IAM key-metadata lookup matched quarantined filename prefix `bffbadd088` to enabled key `bffbadd088e4b1f7b93dd7fa9cfb58c49cc2a259` on `firebase-adminsdk-fbsvc@tabletalk-s.iam.gserviceaccount.com`. A second enabled key `fb29d68eaf68bef1fe0dcbf58a8862721d3a503e` exists. No private key was read or changed. Establish production/automation usage before rotating or disabling either; this is actionable credential cleanup, not completed revocation evidence.
- Metro correction verification: 19 configuration/environment tests pass; production Expo config/permissions pass; `npm run player:export:ios` passes Hermes, inspectable JavaScript, and the newly included actual EAS `export:embed` path (1,144 modules, 19 assets). Both JavaScript scans pass. The full verification rerun is in progress on this correction.
- Automatic approval review rejected Vercel `env run` for a prepared metadata-only Firebase credential check because it would load the production credential into a local process. The operation did not execute. Explicit user permission is pending for this exact access; do not retry or rotate the possibly active key until that permission or equivalent owner-supplied usage evidence arrives.
- Commit `dab4aa7183b6c4482310feb9b1c4f9df3bfcda41` passes all 13 local `npm run verify` checks: 1,298 root/API/Player tests, 183 Web tests, 85 sales-map tests, and the unchanged eight separate emulator skips. Prior commit `d9faf19` also passed its complete CI run. Exact `dab4aa7` CI is [34028934372](https://github.com/sahusgupta/orbit/actions/runs/34028934372).
- [Third simulator build cc64f693-fdf4-416c-8fdc-c2fcc3b9cd99](https://expo.dev/accounts/saussy/projects/tabletalk-player/builds/cc64f693-fdf4-416c-8fdc-c2fcc3b9cd99), source `dab4aa7183b6c4482310feb9b1c4f9df3bfcda41`, version `1.0.0` build `1`, passed CocoaPods and embedded JavaScript bundling; native compilation is still running at this observation. Added a [manual simulator QA workflow](../../../player-app/.eas/workflows/player-simulator-qa.yml) and [bounded local-profile flow](../../../player-app/.maestro/README.md). The automation has not yet run; it is not passing QA evidence.

## Hosted runtime and simulator verification follow-up

- `dab4aa7` CI [34028934372](https://github.com/sahusgupta/orbit/actions/runs/34028934372) succeeded. Simulator automation commit `2da2c6fc57acc69df9ae0cefbd2dd30e97a5f1a9` was committed/pushed to PR #26; its general [CI 34029362453](https://github.com/sahusgupta/orbit/actions/runs/34029362453) succeeded. The [macOS simulator job 34029362548](https://github.com/sahusgupta/orbit/actions/runs/34029362548) installed dependencies/Maestro and is still running its compiler/QA step at this observation.
- EAS simulator build `cc64f693-fdf4-416c-8fdc-c2fcc3b9cd99` **finished successfully**. Downloaded its 19,422,049-byte archive through the existing EAS session without disclosing its artifact URL. SHA-256: `0e16b22ae0e4bc09bb443b904b7320c90e0a1ab78595304b1d45ecb74b88767c`. WSL Python inspected its binary plists directly from the archive after Windows Python execution was denied. The binary matches `com.orbit.player`, version `1.0.0` build `1`, iPhone-only, minimum iOS `15.1`, SDK `26.0` (`23A339`), Xcode `17A324`, and the dedicated Firebase project/app. Camera is its only usage-description permission; there is no URL scheme, Google Maps bundle, Analytics enablement, or tracking declaration. Fifteen bundled privacy manifests were inspected. [The privacy audit](../../../player-app/PRIVACY_MANIFEST_AUDIT.md) records SDK aggregation and the remaining signed-archive limitation.
- Four existing API characterization files passed 30 tests before changes. New regression cases reproduced hosted-development raw error logging and localhost CORS acceptance. An initial telemetry regression fixture correctly hit the production ban on an in-memory store; it was replaced by a real serializer test with transport doubles, which reproduced the raw-stack problem without changing the database guard. Hosted error logs/storage, CORS/HSTS, dashboard cookies, and check-in-origin fallback now apply the existing hosted-or-production boundary. Focused runtime/version/retention tests passed 37; dashboard/session checks passed 28 before their fix and 7 afterward; check-in-origin tests passed 23 afterward.
- Added `GET /version` with validated full source SHA, uncached responses, and honest 503 when deployment metadata is missing. Operational telemetry/error/update records now receive private server-derived 30-day `expiresAt` timestamps, with corresponding TTL/index-exemption deployment inputs; response shapes remain unchanged. No provider TTL or historical backfill has been applied yet.
- Hosted Player Web configuration now rejects missing production identity/URLs, disabled App Check/sync, emulators, and unreviewed public environment overrides; 21 focused tests pass. Deployment staging preserves this guard and excludes environment files, Git metadata, and generated evidence; both staging tests pass. The existing staging script already produces a self-contained Web artifact, so the provider's root `.` can remain unchanged; do not change it to `player-web` when deploying that staged artifact.
- The first aggregate runtime-fix check failed only API check-JS because the new CommonJS test used `import.meta`. Corrected it to the repository's `createRequire(__filename)` pattern. The next `npm run verify` passed all 13 checks, including 1,305 root/API/Player tests, 204 Web tests, and 85 sales-map tests (eight separate emulator skips). After cookie/origin/staging additions, the final `npm run verify` again passed all 13 checks: 1,308 root/API/Player tests, 204 Web tests, and 85 sales-map tests, with the unchanged eight separate emulator skips. Existing ExcelJS eval and bundle-size warnings remain.
- Created separate Firebase browser client key resource `projects/133175572500/locations/global/keys/orbit-player-web-production`, restricted to the two verified Player Web domain referrers and only Identity Toolkit, Secure Token, Firestore, Firebase Installations, and Firebase App Check APIs. Existing management/native keys are unchanged. Follow-up metadata confirms restrictions. Invalid synthetic-token lookup probes prove the allowed referrer reaches Auth (400 `INVALID_ID_TOKEN`), while disallowed/missing referrers return 403 `API_KEY_HTTP_REFERRER_BLOCKED`; no account was created or read.
- Added five more public Vercel Web production settings: `NEXT_PUBLIC_FIREBASE_API_KEY`, `NEXT_PUBLIC_FIREBASE_PROJECT_ID`, `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`, `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`, and `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`. The initial approval rejection was resolved by rereading exact user Phase 3.12/3.13 and re-verifying project identity/absent names. The browser-visible key moved directly through process memory/stdin, with no local key file or printed value. Rollback before adoption removes only these five additions and the unused dedicated key; there is still no Web deployment change.
- Backed up Firebase Auth domain/provider metadata. Email/password is enabled; its initial domains were `localhost`, `tabletalk-s.firebaseapp.com`, and `tabletalk-s.web.app`. Added only `orbit-player-web-liart.vercel.app` and `orbit-player-web-sahoosegs-projects.vercel.app`, rechecked the unchanged initial list before patching, and verified the exact five-domain result afterward. No provider/account/email-delivery change. Rollback removes only the two new domains.
- Production API CORS preflight was rejected twice by automatic approval review, including after the explicit production-smoke request was reread. The second rejection called attachment-derived authorization untrusted. No request executed. A separate user approval question is pending for three `OPTIONS https://orbitapp-one.vercel.app/player/discovery` requests using the two verified Web origins and `https://untrusted-release-probe.invalid`; no credentials or player data would be sent. Do not retry this operation through another tool while approval remains pending.

## Integrity and rendered-QA follow-up

- Before further publisher changes, `npm run verify` passed all 13 checks with 1,313 root/API/Player tests, 204 Web tests, 85 sales-map tests, and eight separate emulator skips. The later Firestore acknowledgement fix requires a new aggregate run.
- [The security review](../reviews/player-release-security-20260906.md) records four reproduced and fixed defects: Firestore duplicate-receipt transaction ordering, deletion cleanup removing a concurrent newer revision, QR responses crossing a changed staff session, and HTTP-200 Firestore batches containing rejected writes. Each has a regression that failed before its fix. Publisher orchestration tests prove the parent commit marker is withheld after failed projection or cleanup writes.
- iOS v1 now exposes email/password authentication only, because production SMS delivery is not provisioned. Existing phone-authenticated account compatibility remains intact. Onboarding includes explicit VoiceOver labels and disabled/checked states; rendered/authentication tests pass. Updated the four generated privacy/support pages from their source and reviewed the intentional component-digest updates; unchanged style digests continue to pass.
- `npm run player:export:ios` passed after the native email/VoiceOver changes: 1,145 modules, 19 assets, Hermes approximately 5.35 MB and inspectable JavaScript approximately 3.78 MB; both bundle scans and actual `export:embed` passed. That export came from the then-uncommitted follow-up tree, not the older completed EAS simulator artifact.
- `npm run e2e:public` passed eight public routes at desktop/mobile sizes without console failures or horizontal overflow. Its local evidence directory is the OS temporary `orbit-public-site-smoke` directory; the run does not verify a new production legal deployment.
- The first `npm run web:e2e` completed 90 captures/42 interactions but failed five cold homepage content checks. A second run passed all existing assertions but failed two new no-JavaScript assertions because Next's streamed page remains hidden without its reveal script. The hero now has immediately readable server-rendered text/actions, covered by a real server-render test; the outer layout provides a readable no-JavaScript explanation and a static Privacy Policy link. The harness waits for readable content/fonts without relaxing existing content, metadata, layout, or console checks. The final rerun **passed 15 routes, six viewports, 90 captures, and 44 interactions**. Sanitized evidence is in ignored `test-results/player-web/screenshots/`; logs are in ignored `out/release-20260906/`.
- `npm run security:paths`, `npm run check:release-controls`, and `npm run audit:module-graph` pass after these fixes. The graph covers 238 modules/709 relative edges with zero cycles, boundary violations, unresolved imports, or missing packaged runtime files.
- The first aggregate rerun failed only API TypeScript on an incomplete new `crypto.Sign` test double. Replaced that double with a real Sign object whose signing method is stubbed; `npm run typecheck:api` and all 36 publisher tests passed. Final `npm run verify` then **passed all 13 checks**, including 1,328 root/API/Player tests, 205 Web tests, and 85 sales-map tests; eight separate emulator tests remain skipped locally. Existing ExcelJS eval/chunk warnings remain.
- Committed integrity fixes as `b02be44c39c3bfe84de3351057590571bae0ec20` and native/Web/QA fixes as `d9fda7e5a001b96a0a7ac68f4b18fb578536d191`. Reviewed unstaged/staged diffs and ran Gitleaks on each staged patch: 13.57 KB and 16.12 KB scanned, zero findings. These commits are still local while the previous long-running native job is preserved; pushing would cancel that job.
- Added the durable [release manifest and artifact index](../../../player-app/submission/2026-09-06/README.md), distinguishing the current code SHA, older completed EAS simulator artifact, provider rollback targets, actual checks, and unresolved authority/configuration fields.
- The next simulator run targets only the runner's native simulator architecture, reports command elapsed times, and writes compiler output continuously to evidence files. This improves diagnosis if CI cancels or times out. The already-running source `2da2c6f` job still uses the previous script; no success is claimed for that job yet.

## Human gates evidenced so far (still pending)

- Subsequent production Firebase API-key metadata inventory was rejected by automatic approval review, which did not accept attachment-derived authorization. A user question is pending for key resource names/restrictions only in `tabletalk-s` / `133175572500`; no key strings are requested and no rejected inventory was rerouted. Existing native API allowlists remain unchanged.

- EAS `workflow:validate .eas/workflows/player-simulator-qa.yml --non-interactive` returned `Running maestro_test jobs requires a paid plan` (request `966e5cf8-f2d0-4527-8e7f-49cc7b8f60ad`). No plan was purchased and no QA job ran there. Added a GitHub-hosted macOS Release compiler/simulator workflow as the actionable alternative, with no signing or production credentials. Its results remain pending.

- Apple Account Holder: authenticate the intended Caminus Labs, LLC team and configure existing/new signing and submission credentials for `com.orbit.player`. EAS currently has neither. This blocks signed iOS/TestFlight work; no password or MFA code should enter chat or repository files.
- Qualified counsel/venue owner: supply existing approved territorial/venue authority and retention decisions. The repository's example deletion policy is explicitly not approval. Technical preparation continues while these decisions are located.

Remaining deployment/configuration verification, native QA execution, credential cleanup after approved access, and final artifact reconciliation remain engineering work, not completed human-only gates. App Check source integration/provider preparation and native QA automation are implemented; enforcement and successful interaction evidence remain separate requirements.

## Native lifecycle regressions and local emulator evidence

- GitHub simulator [run 34029362548](https://github.com/sahusgupta/orbit/actions/runs/34029362548), source `2da2c6fc57acc69df9ae0cefbd2dd30e97a5f1a9`, concluded **cancelled** at `2026-09-06T12:19:48Z` after the 70-minute job limit. Xcode compilation succeeded. The preserved seven-file artifact includes the unsigned app, source/app metadata, 15 privacy manifests, and prebuild/Pods/Xcode logs. CoreSimulator runtime discovery (`xcrun simctl list runtimes --json`) had not completed; no device directories, JUnit results, or screenshots were produced. The runner was ARM64, not Intel. This is compiler evidence, not successful interaction QA.
- Downloaded [artifact 9989138790](https://github.com/sahusgupta/orbit/actions/runs/34029362548/artifacts/9989138790) to ignored `out/release-20260906/native-qa-34029362548/`. Its app ZIP SHA-256 is `a26a2c0ed536074f41aa2ccb714ca1b0dfdbfd7326d5289347e65a17eef07438`. Metadata confirms `com.orbit.player`, version/build `1.0.0`/`1`, iPhone-only minimum iOS 15.1, Xcode 26.0.1 (`17A400`), and iOS simulator SDK 26.0. All 15 manifests declare no tracking; Camera is the only usage-description permission, and Crashlytics collection is disabled.
- Native QA now inventories runtimes before compilation, labels each operation, and applies bounded subprocess limits: two-minute default, Pods 15 minutes, Xcode 40, boot five, Maestro ten per device. It retains the previously added single-architecture build and live compiler logs. `node --check scripts/run-player-simulator-qa.mjs` passes. No mock test claims to reproduce macOS service behavior; a real macOS rerun remains required. The timeout diagnostic does not prove the underlying CoreSimulator hang is repaired.
- Installed a portable official Temurin JRE 21.0.12.1 only under ignored `out/release-20260906/java21/`. Verified the downloaded archive against upstream SHA-256 `d35f31e712f0fcf6ac5a093edc90204fbff22f720ba3950bd09d331d5e621636` before extraction. No system installation, lockfile, or production configuration changed. With `JAVA_HOME` scoped to this runtime, local endpoints, disabled sync, and `GOOGLE_CLOUD_PROJECT`/`GCLOUD_PROJECT=demo-orbit-release-ci`, **`npm run test:firestore-rules` passed 8/8**. The emulator reported demo-project isolation and stopped cleanly afterward. This closes the earlier missing-local-Java verification gap; aggregate-test emulator skips remain distinct from this successful dedicated run.
- Continued source review reproduced three additional native defects: sign-in provider failures incorrectly attempting account creation, pending deletion claiming server data was already removed, and old/pending membership QR state persisting after a venue switch. The security review records the exact failing-before/passing-after evidence. `npm test -- player-app/src/app/playerPlatform.test.ts player-app/src/features/clubs/MembershipWallet.render.test.tsx player-app/src/features/clubs/ClubRoutes.render.test.tsx player-app/src/data/orbitSyncApi.boundary.test.ts` passed **92 tests**. `npm run player:typecheck` passed. A first rendered-wallet run omitted the mock `Platform` export; that harness error was corrected before obtaining the intended two QR failures.
- After the native auth guard alone, `npm run verify` passed all 13 checks, including 1,334 root/API/Player tests, 205 Web tests, and 85 sales-map tests. The later aggregate after the QR/deletion fixes passed 12 checks but failed one component-source checksum (1,339 unit tests passed, one failed, eight emulator skips). The only wallet-source change is the reviewed venue/player key; its behavioral regressions pass. Updated that single checksum; style digests remain unchanged. `npm test -- src/lib/playerOnboardingPresentation.test.ts player-app/src/features/clubs/MembershipWallet.render.test.tsx` then passed **17 tests**, and `npm run player:release:verify` passed. The final aggregate rerun is recorded below when complete.
- `npm run player:export:ios` passed after all three native fixes: 1,145 modules and 19 assets, approximately 5.35 MB Hermes and 3.78 MB inspectable JavaScript, both source scans and real `export:embed`. This export used the documented then-uncommitted follow-up tree and is not either older compiled simulator archive.
- Added the missing 132-character promotional text and a clearly provisional copyright field to the submission package. Apple-owner confirmation of rights identity/year remains necessary. The first one-off Node length-check command failed due to PowerShell argument quoting; a direct PowerShell length check passed (132/170), and the release contract passed. No metadata was sent to Apple.
- Automatic approval review rejected `git push origin codex/player-attestation-release` for the four then-committed fixes, citing private repository egress and no trusted authorization for that destination. No push occurred. The user approval question remains pending; no alternate transport was used. Later local fixes likewise await a permitted push before new exact-source CI, merge, or deployment.

- Final native-lifecycle `npm run verify`: **all 13 checks passed**, including 1,340 root/API/Player tests, 205 Web tests, and 85 sales-map tests; eight emulator skips remain separate from the successful dedicated local run. The 11.32 KB staged diff passed Gitleaks with zero findings and was committed locally as `50c7842cd9d46384050da805c7cb9b1f6b7f5ab8`. It has not been pushed.
- Continued review found two further waitlist UI defects: historical terminal requests disabled new native club requests, and seated Web players were offered unsupported cancellation. Baselines passed two native and 54 Web tests. Nine new native and one new Web regressions then failed, while the active-request preservation cases passed. After the focused fixes, 27 native/presentation and 58 Web component checks passed. The native component-source checksum was updated only after reviewing the two active-status predicates; style checks remain unchanged. Aggregate/bundle/browser verification follows for this additional source change.
- The waitlist `npm run web:e2e` rerun passed 15 routes, six viewports, 90 captures, and 44 interactions with no failures. `npm run player:export:ios` also passed Hermes/inspectable exports, both bundle scans, and real `export:embed` (1,145 modules, 19 assets). These exports used the then-uncommitted waitlist source, not either older compiled native archive.
- The first waitlist aggregate attempt passed eight checks but five checks could not start: esbuild reported `Cannot read directory "../../../..": Access is denied` (one extra ancestor for the Web configuration). Web/root/sales-map test configuration and the desktop/sales-map builds were affected. No failed assertion was produced. Running the focused 27 native/presentation checks with the same isolated environment outside the filesystem sandbox passed. A permitted full aggregate rerun follows; the sandbox failure is retained in `out/release-20260906/verify-waitlist-final.log`.
- Reviewed the complete staged five-file waitlist patch. Gitleaks scanned 4.33 KB and reported zero findings. The zero-commit count is expected for a staged-diff scan; the nonzero byte count confirms the patch was inspected.
- Refreshed PR #26 metadata after the native job ended: it remains open/mergeable at remote source `2da2c6fc57acc69df9ae0cefbd2dd30e97a5f1a9`, with no reviews, green general CI/GitGuardian, and cancelled simulator QA. Local follow-up fixes are not covered by that remote green CI. No PR mutation, push, merge, or production deployment was performed during this follow-up.
- The permitted final waitlist aggregate **passed all 13 checks**, with 1,353 root/API/Player tests, 209 Web tests, and 85 sales-map tests. Eight emulator skips remain separate from the successful dedicated eight-test local run. Log: `out/release-20260906/verify-waitlist-permitted.log`. The reviewed five-file patch was committed locally as `e4fcd7a36bba5fbdc76431d011421aa859e97aa5`; six local commits now await the pending push approval. The earlier native and browser exports match this code tree but were produced before that commit.

## Explicit push authorization and native CI diagnosis

- The user explicitly authorized pushing the seven local commits only to private `sahusgupta/orbit`, branch `codex/player-attestation-release`, fixing/pushing genuine CI failures, and merging PR #26 after all checks pass. The guarded non-force push succeeded; `git ls-remote` independently confirmed documentation HEAD `9df9a5c418cc34b250a3f2b5884cbb4eaf97a1a0`. The working tree was clean, and the remote-tracking branch was refreshed. PR #26 remains mergeable with no reviews or comments. Its title/body now describe the complete final change and distinguish local, CI, deployment, and signing evidence.
- General [CI run 34057170270](https://github.com/sahusgupta/orbit/actions/runs/34057170270) passed at `2026-09-06T20:18:12Z`, along with GitGuardian. The CI aggregate passed all 13 checks (1,353 root/API/Player tests, 209 Web, 85 sales-map); the separate emulator passed eight tests, iOS export/prebuild/code generation passed, and management/public/Web browser smokes passed. The Web run retained 90 captures and 44 interaction checks. The log is preserved locally as `out/release-20260906/ci-34057170270.log`.
- Native [run 34057170254](https://github.com/sahusgupta/orbit/actions/runs/34057170254) failed at `2026-09-06T20:28:41Z`. Runtime discovery now completed in 37 seconds; Pods took 78 seconds and Release compilation 268 seconds. iPhone 16 boot status exceeded five minutes. iPhone 16 Pro Max booted, installed the app, and ran Maestro, but first launch showed the secure-sign-in cleanup error rather than onboarding. The preserved screenshot and simulator log identify Keychain error `-34018`: the unsigned build has neither `application-identifier` nor `keychain-access-groups`. This is a simulator packaging defect; secure storage correctly failed closed. No app assertion or App Check requirement was relaxed.
- Downloaded [artifact 9996588454](https://github.com/sahusgupta/orbit/actions/runs/34057170254/artifacts/9996588454), 14,210,172 bytes, provider ZIP digest `0640099eae1585233650194f66864d522b698b569ca07d48382c720213da17ea`, to `out/release-20260906/native-qa-34057170254/`. It includes the exact-source compiled app, 15 privacy manifests, the failed JUnit result, screenshot, and device logs. It is failure evidence, not successful QA or a TestFlight archive.
- The focused CI fix enables Xcode's local ad-hoc simulator identity (`CODE_SIGNING_ALLOWED=YES`, `CODE_SIGN_IDENTITY=-`), verifies the resulting signature and generated application identity, and retains that signing evidence. It needs no Apple certificate/team/account. A first-boot timeout permits one shutdown/boot retry of the same disposable device before installing the app; both attempt logs remain. App flows still run once and must pass on both sizes. `node --check scripts/run-player-simulator-qa.mjs`, `npm run player:release:verify`, and diff whitespace validation pass. A mock unit test would not establish simulator signing or CoreSimulator behavior; the actual macOS workflow is the required regression check. The clean local aggregate and remote CI must pass before completion or merge.
- The latest user instruction authorizes read-only Vercel/Firebase inspection but requires an exact mutation/rollback proposal before any credential rotation, production configuration change, deployment, or App Check activation. No such mutation occurred during this continuation. Authorized metadata confirms current deployment/alias mappings, CORS rejection of both real Web origins, native key restrictions, 15 ready indexes covering all 14 required indexes, no active TTL, unchanged rules, and default-Off App Check services. The CORS-only API read found an invalid opaque allowlist entry and obsolete Web host. Vercel sensitive storage prevents retrieving the Admin credential; a later `vercel env run` attempt was rejected by automatic approval for potential secret loading and was not retried. The separate safer API read requested only the non-sensitive CORS setting.
- The first simulator-signing aggregate attempt discovered copied API tests inside two generated deployment dry-run folders under `out/`; those copies produced missing-path and duplicate-test failures. Interrupted that contaminated run (exit 1) and preserved its log as `out/release-20260906/verify-simulator-signing.log`. Moved only those two generated source folders, after checking absolute path boundaries, to `C:/Users/herob/AppData/Local/Temp/orbit-release-api-dry-20260906-9df9a5c/`. No test exclusion or application behavior changed. The clean aggregate rerun is recorded separately.
- The clean signing-fix aggregate passed all 13 checks, including 1,353 root/API/Player tests, 209 Web, and 85 sales-map tests (`out/release-20260906/verify-simulator-signing-clean.log`). Committed and pushed `fd781996964904221fde871699d83ea0381502d4` only to the authorized PR branch; independent `ls-remote` matched. GitGuardian passed; general run 34058735107 and native run 34058735102 started on that SHA.
- A subsequent review of the simulator boundary found that its local-profile flow can fetch hosted public discovery. Current API middleware can perform durable housekeeping on any request, so a production-derived app needs network isolation for this continuation's limited production authority. `gh run cancel 34058735102 --repo sahusgupta/orbit` returned HTTP 403 (`Resource not accessible by personal access token`); cancellation was not successful. The replacement push uses the workflow's existing concurrency cancellation behavior. The new helper redirects the reviewed API/Firebase service hosts to IPv4/IPv6 loopback only on the disposable GitHub runner, requires loopback DNS results before launch, and restores the exact original host file in `finally`. Production URLs and App Check remain unchanged. Seven focused tests pass, covering successful isolation/restoration, empty/IPv4/IPv6 failures, task failure, DNS-flush failure, and existing overrides. The initial sandboxed test invocation could not read Vite configuration; the permitted isolated run passed (`simulator-network-tests-permitted.log`). This does not claim simulator DNS or app-flow success until the actual macOS run passes.
- Committed and pushed isolation fix `3a044fe467fc1510dbfaf256365ffb3a0397b899`; independent remote HEAD verification matched. GitHub confirms the superseded native run was cancelled during compilation, before any app launch. The new local aggregate passed all 13 checks (1,360 root/API/Player tests, 209 Web, 85 sales-map). General [CI 34059009311](https://github.com/sahusgupta/orbit/actions/runs/34059009311) passed the same aggregate, separate eight-test Firestore emulator, 18/18 Expo Doctor checks, production iOS export/native generation, management/public smokes, and Web 90-capture/44-interaction smoke. Logs: `verify-simulator-network.log`, `ci-34059009311.log`. Gitleaks scanned both committed CI fixes (16.38 KB) with zero findings.
- GitHub metadata reported `private=false`, `visibility=public`, contrary to the earlier request's private-repository description. No visibility setting was changed. Reported this immediately, held further publication, and the user explicitly replied **Continue in the public repository**, authorizing further CI fixes and the conditional merge at the existing destination.
- Native [run 34059009275](https://github.com/sahusgupta/orbit/actions/runs/34059009275) failed at `2026-09-06T21:06:20Z`. Signing verification passed and Xcode emitted `FAKETEAMID.com.orbit.player` as the simulator application identity. All reviewed service hosts resolved only to loopback; actual app network logs show refused connections over `lo0` with zero request/response bytes. The original host file was restored. Both devices booted on their first attempt (84/80 seconds). iPhone 16 passed onboarding and age gating, then failed because the harness sought an avatar action absent from the home screen. Its screenshot/hierarchy shows the correct Profile tab. iPhone 16 Pro Max showed a blank content view after native/React/secure-storage startup, without a recorded app exception. These failures are preserved, not counted as successful flows.
- Downloaded [artifact 9997160887](https://github.com/sahusgupta/orbit/actions/runs/34059009275/artifacts/9997160887), 18,142,247 bytes, provider digest `0ecd6bdcb96b292dfbbced36e34851f0ccdda674000bc1fc114cce9829e924b0`. Local evidence is `out/release-20260906/native-qa-34059009275/`; live-log access before completion returned 404, so no intermediate substep was guessed. The app entrypoint had no stable root safe-area provider. The installed provider renders no children until it has insets; the delayed-inset condition reproduced an empty view. Two existing-behavior characterization tests passed, a third startup regression failed before the fix, then all three passed after adding a stable root provider seeded with initial window metrics above the unchanged recovery boundary. No app-flow timeout was extended. The Maestro selector now targets the observed Profile tab. The combined root-render/network-isolation run passed ten tests; real macOS validation is still required for this follow-up.

- Startup-fix local verification passed all 13 checks (1,363 root/API/Player tests, 209 Web, 85 sales-map); general [CI 34060414007](https://github.com/sahusgupta/orbit/actions/runs/34060414007) passed the same aggregate, separate eight-test emulator, Expo Doctor/export/native generation, and management/public/Web smokes. Logs: `verify-app-root.log` and `ci-34060414007.log`.
- Native [run 34060414008](https://github.com/sahusgupta/orbit/actions/runs/34060414008), attempt 1 on `0aef72cf6bacba2f0f1df0bfa0c31b58a2bf82c3`, failed only because iPhone 16's XCTest driver exceeded its initial readiness limit before any Orbit flow. Pro Max passed the complete 161-second flow, including startup, onboarding/adult gate, background/foreground, restoration, deletion cancellation/confirmation, and empty state after relaunch. Its five untouched 1320x2868 captures and passing JUnit are preserved in [artifact 9997632927](https://github.com/sahusgupta/orbit/actions/runs/34060414008/artifacts/9997632927), 19,606,845 bytes, provider digest `1410110914c2fa91356b59ea7cf00ed14ddf43aae6b8b55e6e13814db9f4d6c3`. App ZIP SHA-256 `a36e62eea994a9745fc42b986947597ac34e3737bb223495c300ef8c113fc5a5`; ad-hoc identity, 15 manifests, no tracking/Google Maps manifest, and camera-only permission remain verified. A same-source infrastructure retry was accepted through the connected GitHub app; that connection can rerun jobs despite the CLI token's earlier cancellation restriction.
- The preserved XCTest log shows its first HTTP server starting at 21:29:21Z, approximately 131 seconds after driver startup. Maestro 2.7.0 source confirms a 120,000ms default. Set only `MAESTRO_DRIVER_STARTUP_TIMEOUT=180000` in the disposable CI job; app-flow assertions and the overall ten-minute command limit remain unchanged. Actual macOS confirmation is still required.
- Visual inspection of the successful Pro Max captures found the onboarding brand clipped on both fresh and post-deletion launches. A disposable C++ reproduction compiled the installed React Native Yoga engine: the padded container plus its child's 100% minimum height placed the header at -6 points; removing only that child constraint yields +22 points, with the centered body unchanged. The focused layout correction removes that single minimum height. Updated only the reviewed onboarding style digest; the initial focused run passed 12 tests and failed that expected checksum. Native screenshot review remains the visual regression check; a style-value mock would not prove this layout. Evidence: `out/release-20260906/onboarding-layout.cpp`, `onboarding-layout.log`, and `onboarding-layout-tests.log`.

- Pushed `06342eba9bdb2eadfdf99541013cab6a17522ded` only to the authorized branch; independent remote verification matched. Its full local `npm run verify` passed all 13 checks (1,363 root/API/Player tests, 209 Web, 85 sales-map; separate emulator skips unchanged). Focused onboarding tests passed 13. The reviewed staged patch passed Gitleaks (3,675 bytes, zero findings). General CI 34062115348 and native CI 34062115347 were started; native remained pending at the next observation. The earlier same-source infrastructure retry remains separate from this newer code.
- Native screenshot review found a low-contrast deletion action. Following its actual styles also exposed a light ID-preview background paired with light theme text, and the same unreadable red in age and stale-data warnings. Extended the existing DOM-backed native composition tests to preserve actual paint values and measure text against its composed ancestor background using [WCAG 2.2 contrast](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html). Five regressions failed before product edits: preview 1.02:1, warnings/deletion 2.67:1. The focused fix uses the existing dark panel and coral tokens; measured ratios are 16.36:1 for preview values, 6.21:1 for labels, and 6.52:1 for warnings/deletion. The exact unrounded ratios are retained in `native-contrast-ratios.json`. All 23 rendered checks then passed; three reviewed presentation digests were updated after inspecting only the intentional color changes, and all 35 focused checks passed. Existing camera-permission, repeated-scan, sanitized-submit, underage/unavailable-age denial, stale-data refusal, and deletion callbacks remain covered. No account, scanner, or data behavior changed. Logs: `native-contrast-red.log`, `native-contrast-first-green.log`, `native-contrast-green.log`.

- Contrast-fix source `4ed7b7aa6c76e753bd85908d2f8f54298dbb8ab6` passed all 13 local verification checks and general [CI 34062466220](https://github.com/sahusgupta/orbit/actions/runs/34062466220), including the separate eight-test rules emulator, native export/generation, and browser smokes. GitGuardian passed. Native simulator QA remained in progress.
- Added the requested text-scaling QA at system `accessibility-large` on the smaller iPhone. The same complete local flow runs again with separate JUnit/screenshots; the harness reads back the setting and fails on mismatch. Scrolling reaches expanded form controls without removing any assertion. Syntax/YAML validation and all seven isolation tests passed. No production app code, font scaling, security, or network-isolation setting changed. Native confirmation remains pending; local aggregate verification is running.

- Text-scaling source `811040cdb3d2e85d4fb197367b485eb96230548e` passed all 13 local verification checks and general [CI 34063114279](https://github.com/sahusgupta/orbit/actions/runs/34063114279). The dependency gate passed with zero root/API/Web advisories and eight existing reviewed Player build-tool High advisories, expiring September 30; no exception was added. The text-scaling staged patch scanned 3,340 bytes with zero Gitleaks findings.
- Native [CI 34063114409](https://github.com/sahusgupta/orbit/actions/runs/34063114409) passed all three complete local flows, including `accessibility-large`, with 15 original RGB screenshots. Each device used its existing single pre-install boot retry; app assertions were not retried. Signature, application identity, 15 privacy manifests, camera-only permission, no tracking, loopback isolation, and host-file restoration were verified. [Artifact 9998605299](https://github.com/sahusgupta/orbit/actions/runs/34063114409/artifacts/9998605299) is 29,476,582 bytes, provider digest `e7b366accafaf52dc5352d7e55582f4e0996b1d61dc2fd104a4cb95c321c9c54`; app ZIP SHA-256 `392cf7839d435531302174c2bbf820dec7fce0b9e15cf76abd92a14511dec65c`. Visual inspection confirms the header and deletion-contrast fixes; five Pro Max store-dimension candidates were copied without modification to ignored local evidence.
- Merged PR #26 with the repository's normal squash method and an expected-head guard. Actual merge `9458a6a50567c879b537631b9b25fb76e89b4133`, at `2026-09-06T22:45:47Z`, independently matches remote main. Reviewed head, CI synthetic merge, and actual merge share tree `f612275e3bf74c9f49d5ed35a4356411f98cfb9a`. The working branch remains the authorized release branch. Main CI 34064957418 is being monitored.
- Exact-merge API/Web archives were prepared outside the repository. Two initial packaging assertions exposed Windows Git line-ending conversion; normalized content matched, and per-command LF export plus immutable Git-blob comparisons resolved the check without editing either lockfile. API archive SHA-256 `641a57a65c7666974946f2b0eef756bd5b1348517f39e7e4861aad36bfafec0c`; Web source archive `20e15f5822ffefa5e9ef69f024af40f5c78446976ae60f9ecebb66bc1c816265`. The API dry-run passed for 113 files / 1,290,802 bytes, including root branding and the separately hashed proposed metadata diagnostic/configuration. Nothing was uploaded. Isolated Web install passed with zero reported advisories; its build is running.

- Actual-merge CI 34064957418 passed on `9458a6a50567c879b537631b9b25fb76e89b4133`. Its isolated standalone Web build then found a packaging defect that in-repository builds masked: shared `playerSync` and `membershipQr` import the omitted portable secure-identifier generator and validator. The new real staged-bundle regression failed before the fix (one failed / one passed), then both staging tests passed after including those two existing source modules. No application source or dependency version changed.
- Joined the identical merge tree into the same authorized branch without overwriting the local handoff edits, committed `113823ca1f7bad430bbb5943559533139126b654`, and pushed normally to `codex/player-attestation-release`. [PR #27](https://github.com/sahusgupta/orbit/pull/27) contains only the staging helper, its regression, and staging documentation: three files / 25 insertions. Gitleaks scanned 1,392 bytes with zero findings. Full verification, corrected standalone build, general CI 34065375262, and native CI 34065375278 are running. The first API candidate approval remains explicitly scoped to the prepared PR #26 merge; no deployment occurred.

- The corrected standalone Next build passed after adding the two portable security modules directly from immutable PR #26 merge blobs. The existing lockfile install reported zero advisories. This is isolated packaging proof with loopback API and disabled client sync/App Check, not a deployable production-configured prebuilt bundle. Log: `out/release-20260906/merged-web-build-corrected.log`. Preserved first-candidate provenance as `merged-packaging-9458a6a.json` so the pending exact-source API proposal is not silently changed by later packaging work.

- Full local verification on packaging fix `113823ca1f7bad430bbb5943559533139126b654` passed all 13 checks (1,363 root/API/Player tests, 209 Web, 85 sales-map). The API subtree exactly matches PR #26 merge: `87570aa5b2e8bce56c11161fabebdf319e23a74f`. The pending API-candidate approval remains fixed to that earlier merge, staging directory, and hashes.

- PR #27 native run `34065375278`, attempt 1, failed only because the iPhone 16 CoreSimulator boot and its existing retry each timed out after 300 seconds, before Orbit was installed. Xcode compilation passed; the Pro Max complete flow passed one JUnit case with zero failures in 163 seconds. Enlarged-text testing could not run on the unavailable first simulator. Hosts were restored. Preserved artifact `9999112340` (20,104,830 bytes; digest `94f8c0666156a52e52832ae83b9d72b2ab0eadf7cb79edfaf3f667ba7f0c209c`) and `native-failed-34065375278.log`. Requested one unchanged-job retry on a fresh runner: job `101576461740`, started `2026-09-06T23:21:23Z`. No product assertion, timeout, or source code was changed for the retry.

## Current handoff: green follow-up awaiting exact merge approval

- PR #27 native run `34065375278` attempt 2 passed on `113823ca1f7bad430bbb5943559533139126b654`: three JUnit cases, zero failures/errors, 15 PNG captures. Both simulators booted first try (57/58 seconds); no app assertion or timeout changed. Xcode build `17A400`, app ZIP SHA-256 `002dfa56ef4bee6203f159db99e1169e2a83bf0d61daf652594b8d3d07401251`, 15 privacy manifests identical to the prior reviewed inventory, no tracking/Google Maps manifest, camera-only permission. Artifact `9999461703` is 30,433,184 bytes with digest `85beb43afc76f73c567c4d3e3c87ffd82fa3597fad766ff618898c23f9c6cb19`. Five original Pro Max captures were copied without modification into `store-capture-candidates-113823c`; enlarged age-gate and Pro Max deletion captures were visually inspected.
- General CI, GitGuardian, and native QA all passed; PR #27 was MERGEABLE/CLEAN with no reviews or review requests. The expected-head squash merge was **rejected before execution by automatic approval**, which considered explicit merge authority limited to PR #26. A direct approval question now names PR #27, target main, and exact head. No alternate merge route was attempted. PR #26 remains the actual merged release source.
- Archived exact PR #27 head into isolated `C:/Users/herob/AppData/Local/Temp/orbit-candidate-113823ca1f7b-wF0SbA`, explicitly recording `mergeSha: null`. Web staging has 164 files and no forbidden files; both locks match their Git blobs. API locked install passed (261 packages, zero advisories) and `node --check` passed without starting the API. First API-candidate proposal inputs remain unchanged.
- The handoff and provider/security findings remain local; no additional public upload of those findings is implied by the authorized code pushes. No production mutation, Apple signing, TestFlight upload, or App Review submission occurred in this continuation.

- Fresh standalone Web staging from exact PR #27 head passed locked installation (569 packages, zero advisories) and the actual Next.js build. Logs: `candidate-web-install-113823c.log` and `candidate-web-build-113823c.log`. This local build used loopback API and disabled sync/App Check; it is not a production-configured prebuilt upload. No PR #27 merge or deployment is implied.

## PR #27 merge authorized and verified - September 7 UTC

- The user explicitly instructed "merge it." Refreshed PR #27: expected head `113823ca1f7bad430bbb5943559533139126b654`, base `9458a6a50567c879b537631b9b25fb76e89b4133`, MERGEABLE/CLEAN, all three checks SUCCESS, no reviews or requests. Expected-head squash merge succeeded at `8d086bfd689b7f0af1037468c0764d16b32d77bc`, timestamp `2026-09-07T00:37:51Z`; independently verified remote main and fetched that exact commit without changing the working branch. The earlier automatic merge rejection is resolved.
- Actual merge and reviewed head share tree `ab532ae7b35edb12aebf27c9b4662951818e2ced`. Regenerated capture provenance using that verified equality, preserving the fact that the simulator binary was compiled from the PR head. Main CI `34070422400` is running on the actual merge.
- Prepared fresh API/Web archives from the exact merge under `C:/Users/herob/AppData/Local/Temp/orbit-release-8d086bfd689b-ddzeYB`; both lock blobs match. API archive SHA-256 `066361a75ddb30344f924bca35fc9e3ea36cd4c8266aff67b8ce89d765afc795`; Web archive `72cfa2a7dfc89d381842e4eae1214fa124773bb71f753bcb91018c4c847225a2`. Web staging contains 164 files and no forbidden files. The first API-candidate proposal remains unchanged at the earlier merge and still requires its own explicit production approval.

- Fresh locked Web install from the exact PR #27 merge passed (569 packages, zero advisories), and the standalone Next.js build passed. Logs: `merged-web-install-8d086bf.log` and `merged-web-build-8d086bf.log`. API remained loopback and client sync/App Check disabled. Release contract, 32 local links, manifest source/proposal consistency, and whitespace checks passed. Main CI remains pending below.

- Actual-merge [CI 34070422400](https://github.com/sahusgupta/orbit/actions/runs/34070422400) passed on `8d086bfd689b7f0af1037468c0764d16b32d77bc`: all 13 verification checks, 1,363 root/API/Player tests, 209 Web tests, 85 sales-map tests, separate eight-test rules emulator, Expo Doctor 18/18, production native export/prebuild, and management/public/Web browser smokes. The reviewed dependency policy passed with its existing September 30 review deadline. Saved full log `out/release-20260906/ci-34070422400.log`. All requested PR #27 merge/source checks are complete; the separate production proposal, Apple credentials, accountable declarations, and unavailable device testing remain open.

## Approved API candidate attempted and rolled back - September 7 UTC

September 7 API attempt: the user approved the first candidate. Deployment `dpl_4KoTNHWQboGvHv14aguYMCNDVQzx`, from PR #26 merge `9458a6a50567c879b537631b9b25fb76e89b4133`, reached READY on Node 22.x, but Vercel reassigned the secondary API alias despite `--skip-domain`. The secondary alias was restored to its exact prior deployment and the candidate deleted. All four aliases and saved API build settings match the original snapshot at `2026-09-07T01:26:25.641Z`. No runtime requests were sent by this run; incidental traffic or data effects during the brief secondary-alias assignment are not established by the control-plane evidence. No credential diagnostic output was observed. The revised diagnostic was subsequently approved, completed, and deleted; see the latest execution record.

- Upload inventory and SHA-1 for all 113 files / 1,290,802 bytes matched the approved dry run. The sole environment template was the exact committed `apps/api/.env.example`; no live environment file was uploaded. Five recorded archive/diagnostic/config/lock/branding SHA-256 values matched.
- Exact target: API `prj_2EEyDB2YImgK9dil3GK79Qy0UfVw`, team `team_CdmC6YZS1GNIoI0waQ2G01ED`. Command used the approved stage, explicit staged `--local-config`, `--prod --skip-domain`, the two approved deployment-only environment overrides, and `orbitReleaseSha` metadata. No saved environment values were changed.
- Created at `2026-09-07T01:23:56.450Z`; READY at `2026-09-07T01:24:20.737Z`. Immutable URL was `orbit-2z4qop31u-sahoosegs-projects.vercel.app`. Locked provider install added 261 packages, audited 262, and reported zero vulnerabilities. Provider metadata selected Node `22.x`; the exact patch was not captured. The requested build command was recorded in deployment settings, but its three-field diagnostic was absent from observed logs. The Admin credential identity remains unknown.
- Post-build alias verification failed for `orbitapp-sahoosegs-projects.vercel.app`; the canonical API and both Web aliases matched their recorded targets. Restore command: `vercel alias set orbit-f6o4olsbb-sahoosegs-projects.vercel.app orbitapp-sahoosegs-projects.vercel.app --scope sahoosegs-projects` succeeded. Then `vercel remove dpl_4KoTNHWQboGvHv14aguYMCNDVQzx --scope sahoosegs-projects --yes` removed exactly one deployment. Later inspect returned `Can't find the deployment`. No second deployment was created.
- Evidence: `api-candidate-before.json`, `api-candidate-4KoTNHWQ-metadata.json`, `api-candidate-after.json`, and `api-candidate-deletion-verified.json` in ignored `out/release-20260906/`. These contain selected metadata only. No raw managed credential was read locally or printed.
- Local diagnostic retry preparation passed four synthetic tests, including the expected nonzero exit after exactly three identifiers, plus a dry run of 113 files / 1,290,951 bytes. It is a proposed intentionally failed metadata build, not an API release. No application or lockfile source change was made. Existing full verification on the unchanged merged source remains green. Four diagnostic safety tests, the release-document contract, 32 local documentation links, manifest rollback assertions, and whitespace checks passed for this local evidence update; full verification was not rerun for documentation-only changes.

## Approved diagnostic completed and removed - September 7 UTC

The user approved the revised diagnostic-only build. It ran from PR #26 merge `9458a6a50567c879b537631b9b25fb76e89b4133`, stopped during installation with the intended exit 42, and was deleted. All four live aliases and saved API build settings remained unchanged. The current managed Vercel credential belongs to `tabletalk-s`, service account `firebase-adminsdk-fbsvc@tabletalk-s.iam.gserviceaccount.com`, key `fb29d68eaf68bef1fe0dcbf58a8862721d3a503e`. Its email was redacted in the observed build log; the service-account identity was independently established by matching the exact key ID to its IAM resource. No secret value was returned, and no runtime request was sent. The older quarantined key `bffbadd088e4b1f7b93dd7fa9cfb58c49cc2a259` is separate, still enabled, and has recorded recent authentication; its consumers remain unresolved. Both diagnostic approvals are complete. Further deployment/routing, runtime testing, credential mutation, and App Check remain outside those approvals.

- Deployment `dpl_A6qRPz4VEbHuxfi23ViEGVSEuvhz`, URL `orbit-q79iz6vvn-sahoosegs-projects.vercel.app`, target production, source metadata `9458a6a50567c879b537631b9b25fb76e89b4133`; Node `22.x`. Created `2026-09-07T03:58:12.481Z`. The installation command ran at `2026-09-07T03:58:16.688Z` and exited 42 at `03:58:16.739Z`. Provider ERROR is the intended diagnostic stop, not an application build or CI failure. The API was not installed, packaged, or probed.
- All 113 reviewed files / 1,290,951 bytes and diagnostic/config/lock hashes matched before upload. The supplied deployment settings retained both approved commands. `aliasAssigned=false`; the system alias listed as a prospective target was never assigned. Deletion removed exactly one deployment and subsequent inspect could not find it. All four aliases and saved API settings matched at `2026-09-07T04:01:11.281Z`.
- The first log parser could not parse a line containing a redacted `client_email`; extracting only validated project/key fields resolved it without retrieving the redacted value. IAM key metadata matched the full key ID to the service account. Current managed key created July 17, 2026; older quarantined key created June 29. Both have `disabled=false`. This identifies the current managed production variable, not every older immutable deployment's frozen credential.
- Read-only Cloud Monitoring key-authentication counts from June 1 through `2026-09-07T04:02:54.440Z`: older key 9 events in 3 daily buckets, latest positive bucket ending `2026-09-01T04:02:54.440Z`; current key 57,709 events in 28 daily buckets, latest positive bucket ending `2026-09-06T04:02:54.440Z`. Bucket boundaries are not exact last-use timestamps, and absence of later metrics is not proof of inactivity. Do not disable/delete the older key without resolving its consumers and separately approving the mutation/rollback.
- An audit lookup initially rejected nested service/method projection with HTTP 400. A narrower timestamp/log-name query over the old key's latest positive day completed in one page with zero entries; that does not negate authentication metrics or identify all consumers. No customer log payload, document path, IP, or token was requested.
- Aggregate Vercel function-invocation query for the first candidate's brief alias window returned `payment_required`: Observability Plus is required. No plan or billing change was made. Incidental traffic/data effects from the first attempt remain unverified.
- Selected evidence: `api-diagnostic-approved-metadata.json`, `api-diagnostic-approved-before.json`, `api-diagnostic-approved-after.json`, `api-diagnostic-approved-deletion.json`, `firebase-key-usage-after-diagnostic.json`, `firebase-old-key-audit-metadata.json`, and `first-api-attempt-invocation-metrics.json`, all under ignored `out/release-20260906/`. Provider/security handoff remains local. No source/lockfile change or Git push occurred in this continuation.

## Retained API candidate proposal prepared - September 7 UTC

Read-only Vercel protection/domain metadata indicates generated URLs should require Vercel Authentication; this was not runtime tested. The known automatic secondary-alias reassignment is now explicitly included in the proposed scope, with immediate exact restoration and deletion of only the new candidate on failure. No production deployment or runtime request occurred. Final merge `8d086bfd689b7f0af1037468c0764d16b32d77bc` was staged directly from its verified immutable archive; the only staged configuration change is `installCommand: npm ci`. CLI dry run matched all 112 upload files / 1289762 bytes by hash. The exact command, transient-routing/data limits, and rollback are in [the production proposal](player-production-mutation-proposal.md#approved-retained-api-candidate-completed). This third deployment is prepared and **not approved**. Existing source verification remains green; source and locks are unchanged.

Preparation checks passed: `npm run player:release:verify`, 32 local documentation links, JSON/source/rollback assertions, and `git diff --check`. The CLI help command returns exit 1 while displaying help; the actual dry run returned 0. The four-file preparation diff was reviewed against pre-update backups. Full verification was not repeated for unchanged application source; existing local verification and exact-merge CI remain the applicable evidence. Eight handoff files remain local and unstaged on `codex/player-attestation-release`; no new commit or push occurred.

## Approved merged API candidate retained - September 7 UTC

The user-approved retained API candidate is READY: `dpl_3ChFg6JQ9e3DjfGpwT2vMe9iskcZ`, immutable `orbit-l5lr0hmcw-sahoosegs-projects.vercel.app`, from final merge `8d086bfd689b7f0af1037468c0764d16b32d77bc`. Locked installation and the API build passed on Node 22.x; the provider added 261 packages, audited 262, and reported zero vulnerabilities. All four live aliases and saved API/protection settings match the preflight. No alias movement was observed, no restore command was needed, and final candidate alias metadata is empty. No runtime request or canonical cutover was performed.

- Approval: the user explicitly said `i approve the prepared API deployment and rollback`. Source/config/lock hashes and all 112 files / 1,289,762 upload bytes matched the reviewed inventory. Existing local source tests and exact-merge CI remain applicable; no application or dependency source changed. Four isolated alias-restoration guard checks passed before execution.
- Created `2026-09-07T04:48:17.520Z`; READY `2026-09-07T04:48:41.700Z`. Build metadata records `npm ci`, Node `22.x`, source metadata matching the merge, and one 3.43 MB `index` function in `iad1`. The exact provider Node patch was not captured.
- Control-plane polling observed BUILDING twice and then READY. The secondary alias never differed in those checks; no restoration was invoked. The final deployment has `alias: []`, while `automaticAliases` lists the secondary alias and `aliasAssigned: true`. Those bookkeeping fields alone do not prove routing; the authoritative alias mappings still target their original deployments. This does not erase the first attempt's different observed behavior or prove no brief unobserved transition occurred.
- All four aliases and saved API build settings match at `2026-09-07T04:50:16.401Z`; protection scope/domain configuration matches as well. No runtime request, promotion, secret rotation, saved configuration mutation, Firebase change, or App Check activation occurred in this operation. The candidate is retained under the approved scope. No zero-invocation claim is made.
- Evidence under ignored `out/release-20260906/`: `retained-api-approved-execution.json`, `retained-api-approved-metadata.json`, `retained-api-approved-final-metadata.json`, `retained-api-approved-build-evidence.json`, `retained-api-approved-before.json`, `retained-api-approved-after.json`, and both `retained-api-approved-settings-*.json`. Historical attempts remain separately recorded.

Final control-plane audit at `2026-09-07T08:57:37.578Z` reconfirmed this same READY candidate, source SHA, all four original alias targets, and unchanged protection/build settings; no runtime request was sent. Handoff verification passed: `npm run player:release:verify`, 32 local documentation links, manifest/source/rollback assertions, and whitespace checks. The seven updated handoff diffs were reviewed; existing full local verification and actual-merge CI remain the evidence for unchanged application source. Eight handoff files remain local and unstaged, with no new commit/push. The next eight runtime requests and exact failure-removal target are prepared in the production proposal and remain unapproved. Existing automation access was confirmed through metadata only; no new bypass or provider configuration was created.

## Initial runtime runner completed locally - September 7 UTC

The previous goal turn completed and verified the retained deployment. While the new runtime approval remained pending, the exact eight-request runner was implemented and passed 20 offline checks at `2026-09-07T09:00:55.662Z`; zero provider or runtime requests were made. Its result filtering excludes cookies, unknown headers, arbitrary bodies and unexpected JSON fields; it rejects wrong source/cache/CORS behavior, changed targets, extra requests and missing approval. The local curl configuration is isolated without reading user settings, and execution is journaled to prevent automatic retry. Runner SHA-256 `356f08e4d725fec324fdfe2f7dd525397c9389bba1a91b3806ced648bf042fa0`; evidence `out/release-20260906/retained-api-runtime-runner-tests.json`. This completes the local execution harness, not production smoke testing. The approved/execute flags remain false.
