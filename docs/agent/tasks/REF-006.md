# REF-006: Split renderer route sections into typed feature components

Status: `active`

Date: 2026-08-07

## Objective

Move each large route-owned JSX branch from `src/main.tsx` into a typed feature component while retaining top-level state, effects, commands, route selection, shell composition, and mutation orchestration until separately characterized.

## Execution constraints

- Extract one route and one committed characterization boundary per subtask.
- Preserve DOM structure, classes, labels, control order, event timing, and callback semantics.
- Pass typed values and callbacks; feature components must not gain persistence, Firebase, Electron bridge, or global-state ownership.
- Keep `AppShell` composition in `main.tsx` during the first route moves so navigation and notification behavior remain centralized.
- Do not combine route extraction with styling, data redesign, callback rewrites, or opportunistic cleanup.

## Route sequence

| Subtask | Route | Status | Characterization |
| --- | --- | --- | --- |
| REF-006A | Outreach/signals | complete | `src/lib/groupMeCandidates.test.tsx` |
| REF-006B | Tonight/builder | complete | `src/lib/plannedParticipants.test.ts` plus existing table-planning coverage |
| REF-006C | Profiles | complete | `src/lib/profileImport.test.tsx` plus existing profile relationship coverage |
| REF-006D | Tournaments | complete | `src/lib/tournamentView.test.tsx` plus existing tournament domain coverage |
| REF-006E | Summary | complete | `src/lib/tableEventReporting.test.ts` plus reporting and night-close coverage |
| REF-006F | KPIs | complete | `src/lib/tableEventReporting.test.ts` plus analytics/reporting coverage |
| REF-006G | Customization | ready | account/license/settings suites plus route DOM fixture |
| REF-006H | Table | planned | table lifecycle/transition/collection suites plus route DOM fixture |
| REF-006I | Floor/default | planned | floor rendering, quick-add, waitlist, seating, and planning suites plus route DOM fixture |

The tournament-TV route already delegates to `TournamentTvView`; this task only preserves its existing wrapper in `main.tsx`.

## REF-006A characterization

Before extraction, the GroupMe candidate suite now pins the unchanged Outreach route heading/subtitle, navigation order/current marker, panel order, textarea contract, all four template values, generated script order, and primary controls. Its existing cases continue to pin scan/edit/accept/reject behavior and timestamps.

Acceptance for REF-006A requires the same three cases after rendering a typed `SignalsView`, zero TypeScript diagnostics, and full repository verification.

## REF-006A implementation and verification

- Added typed `src/components/SignalsView.tsx`; it owns only the preserved route markup and candidate edit wiring passed through callbacks.
- Added shared `src/components/PanelTitle.tsx` and replaced the identical local helper without changing its markup or toggle contract.
- `src/main.tsx` retains `AppShell`, route selection, all state, persistence, scanning, accept/reject, template updates, clipboard usage tracking, and navigation callbacks.
- The characterized Outreach suite passed 1 file/3 tests; both root compiler projects passed with zero diagnostics; Player TypeScript, all 37 files/199 tests, the 1,922-module renderer build, and `npm run verify` passed.
- `src/main.tsx` decreased from 8,342 to 8,209 lines. No DOM class/label/control order, state shape, callback semantics, external service, or deployment changed.

## REF-006B characterization

Before extraction, the planned-participant suite now pins the unchanged Tonight heading/subtitle, navigation order/current marker, filter order and options, panel headings, top-bar actions, and primary builder control. Its existing cases continue to pin ranked participant rendering and planned-table persistence, while the table-planning suite covers the adjacent table mutation boundary.

Acceptance for REF-006B requires the same four planned-participant cases and four table-planning cases after rendering a typed `BuilderView`, zero TypeScript diagnostics, and full repository verification.

## REF-006B implementation and verification

- Added typed `src/components/BuilderView.tsx`; it owns only the preserved route markup, presentation filtering, and callback wiring.
- `src/main.tsx` retains `AppShell`, route selection, all state, demand/viability domain operations, planned and balanced table mutations, persistence, report export, and navigation callbacks.
- The affected suites passed 2 files/8 tests; both root compiler projects passed with zero diagnostics; Player TypeScript, all 37 files/200 tests, the 1,923-module renderer build, and `npm run verify` passed.
- `src/main.tsx` decreased from 8,209 to 8,069 lines. No DOM class/label/control order, state shape, callback semantics, external service, or deployment changed.

## REF-006C characterization

Before extraction, the profile-import suite now pins the unchanged Players heading/subtitle, header tool order and accessible labels, membership tab order/counts/current marker, directory health labels, panel order, search placeholder, and import controls. Its existing cases continue to pin pasted profile normalization and persistence, while the profile-relationship suite covers deletion, merging, check-in identity resolution, and relationship preservation.

Acceptance for REF-006C requires the same five profile-import cases and eight profile-relationship cases after rendering a typed `ProfilesView`, zero TypeScript diagnostics, and full repository verification.

## REF-006C implementation and verification

- Added typed `src/components/ProfilesView.tsx`; it owns the preserved route markup and local form/control wiring without introducing `any` annotations.
- `src/main.tsx` retains `AppShell`, route selection, all state, QR scanner device controls, imports, relationship resolution, membership approvals/activation, profile and interest mutations, persistence, and navigation.
- The affected suites passed 2 files/13 tests; both root compiler projects passed with zero diagnostics; Player TypeScript, all 37 files/201 tests, the 1,924-module renderer build, and `npm run verify` passed.
- `src/main.tsx` decreased from 8,069 to 7,545 lines. No DOM class/label/control order, state shape, callback semantics, external service, or deployment changed.

## REF-006D characterization

Before extraction, a dedicated tournament route harness now pins the seeded library heading, new-tournament controls, library card content/action, transition into creation, setup heading, six-field order, and form action order.

Acceptance for REF-006D requires the same library-to-create case after rendering a typed `TournamentsView`, zero TypeScript diagnostics, and full repository verification.

## REF-006D implementation and verification

- Added typed `src/components/TournamentsView.tsx`; it owns only the preserved tournament-manager markup, draft field wiring, and callback selection without introducing `any` annotations.
- `src/main.tsx` retains `AppShell`, route selection, all tournament state, creation/editing, clock transitions, entries, table draws, payouts, persistence, and TV-window launch.
- The characterized suite passed 1 file/1 test; both root compiler projects passed with zero diagnostics; Player TypeScript, all 38 files/202 tests, the 1,925-module renderer build, and `npm run verify` passed.
- `src/main.tsx` decreased from 7,545 to 7,430 lines. No DOM class/label/control order, state shape, callback semantics, external service, or deployment changed.

## REF-006E characterization

Before extraction, the table-event reporting suite now also pins the unchanged Reports heading/subtitle, export action order, report-mode navigation/current marker, period tabs/current marker, profit breakdown, numerical metric order, and KPI category controls. Its existing cases continue to pin event-reason projection and exact CSV export behavior.

Acceptance for REF-006E requires the same three reporting cases plus reporting-domain and night-close coverage after rendering a typed `SummaryView`, zero TypeScript diagnostics, and full repository verification.

## REF-006E implementation and verification

- Added typed `src/components/SummaryView.tsx`; it owns the preserved report/night-close markup and display-only aggregations without introducing `any` annotations.
- `src/main.tsx` retains `AppShell`, route selection, report projections, all state, night-close reconciliation/sign-off/locking, persistence, export, settings navigation, and low-light mutation callbacks.
- The affected suites passed 3 files/11 tests; both root compiler projects passed with zero diagnostics; Player TypeScript, all 38 files/203 tests, the 1,926-module renderer build, and `npm run verify` passed.
- `src/main.tsx` decreased from 7,430 to 7,012 lines. No DOM class/label/control order, state shape, callback semantics, external service, or deployment changed.

## REF-006F characterization

Before extraction, the reporting harness now also navigates to the KPI route and pins the unchanged eyebrow/heading, export action order, and all ten owner-metric labels in order.

Acceptance for REF-006F requires the same four reporting-harness cases after rendering a typed `KpisView`, zero TypeScript diagnostics, and full repository verification.

## REF-006F implementation and verification

- Added typed `src/components/KpisView.tsx`; it owns only the preserved KPI markup and contains no state or `any` annotations.
- `src/main.tsx` retains `AppShell`, route selection, analytics computation, CSV export, and close navigation.
- The characterized reporting suite passed 1 file/4 tests; both root compiler projects passed with zero diagnostics; Player TypeScript, all 38 files/204 tests, the 1,927-module renderer build, and `npm run verify` passed.
- `src/main.tsx` decreased from 7,012 to 6,953 lines. No DOM class/label/control order, state shape, callback semantics, external service, or deployment changed.
