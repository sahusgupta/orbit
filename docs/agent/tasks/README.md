# Agent Tasks

Store narrowly scoped future engineering task records here. Each task should identify evidence, boundaries, verification, production-safety constraints, and completion criteria.

The temporary TypeScript remediation and compiler-boundary queue is indexed by `docs/agent/TASKS.yaml`. Its specifications are `TYPE-001.md` through `TYPE-022.md`; it is not the broader Orbit refactor plan.
